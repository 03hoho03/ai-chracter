/** 이미지 생성 한시 중단 스위치 (2026-09-01) — 되돌릴 때 손댈 곳은 이 파일 하나다.
 *
 * **왜 막았나.** 백엔드를 Cloud Run request-based 과금(`--cpu-throttling`)으로 옮기면서
 * CPU가 요청을 처리하는 동안에만 할당된다. 이미지 생성은 응답을 보낸 **뒤** `asyncio.create_task`로
 * 도는 fire-and-forget 잡이라(`apps/api`의 `images/router.py` → `enqueue_generation`) 그 CPU
 * 회수 구간에 걸려 끝나지 않는다. 잡은 `running`에 멈춘 채 남고 폴링은 영원히 돌므로, 막지 않으면
 * 사용자가 오지 않을 결과를 기다린다 — 화면이 아무 말도 하지 않는 게 이 상태의 진짜 문제다.
 *
 * **서버는 여전히 요청을 받는다.** 이건 UI 차단이지 기능 제거가 아니다. 이미 만든 이미지의
 * 보관함·선택(`내 이미지` 탭, 빌더의 이미지 피커)은 그대로 동작하므로 건드리지 않았고, 폼 필드도
 * 막지 않았다 — 입력해 둔 프롬프트가 재개 시 그대로 남는 편이 낫다.
 *
 * **되돌릴 때.** 아래 상수를 `false`로 두면 폼이 원래대로 돌아온다. 이미지 생성 잡을 Cloud Run
 * Jobs로 분리했거나 `--no-cpu-throttling`으로 되돌렸다면 이 파일과 유일한 소비처
 * (`ui/GenerateImagesForm.tsx`)의 관련 분기를 함께 지울 것. 배경과 과금 수치는 `DEPLOY.md` §7.
 */

/** 타입을 `boolean`으로 명시하는 건 의도적이다 — `true` 리터럴 타입이면 이 상수를 쓰는 삼항의
 * 반대편과 early return 뒤의 코드가 전부 도달 불가로 좁혀져, 되돌릴 때 되살릴 분기가 타입 단계에서
 * 죽은 코드가 된다. */
export const IS_IMAGE_GENERATION_SUSPENDED: boolean = true;

/** 왜 못 누르는지. 버튼 옆에 두고 버튼이 `aria-describedby`로 가리킨다 — 비활성 사유는 보이는
 * 자리와 접근성 트리 양쪽에 있어야 한다.
 *
 * **한 문장인 건 길이 제약이다.** 이 폼은 `max-w-2xl` 안이라 버튼 줄의 폭이 624px이고 버튼이
 * 85.9px이라 옆에 남는 건 526px뿐이다(1280px 실측). 처음엔 "만들어 둔 이미지는 ‘내 이미지’에서
 * 그대로 쓸 수 있어요"를 붙였는데 그 길이가 남는 폭을 넘겨 `flex-wrap`이 발동, 버튼 **아래로**
 * 접혔다. 지금 문장은 242.4px이라 옆에 앉는다(390px에서도 342px 중 340px로 아슬아슬하게 붙고,
 * 320px에서 비로소 아래로 접힌다 — 셋 다 실측). 갈 곳 안내는 바로 옆 `내 이미지` 탭이 이미
 * 하고 있으니, 여기서는 "왜 못 누르는지"만 말한다. */
export const IMAGE_GENERATION_SUSPENDED_NOTICE = "이미지 생성은 개편 중이라 잠시 멈춰 뒀어요.";
