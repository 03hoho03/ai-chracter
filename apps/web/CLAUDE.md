# apps/web

- FSD 레이어 의존 방향: `app → pages → widgets → features → entities → shared` (역방향 import 금지). `routes/*`는 로직 없이 `pages/{page}`를 렌더링만 하는 얇은 어댑터로 유지한다.
- `src/routeTree.gen.ts`는 TanStack Router의 `@tanstack/router-plugin` Vite 플러그인이 `vite dev`/`vite build` 실행 시 자동 생성하는 파일이며, `tsc --noEmit`만으로는 fresh checkout에서 생성되지 않으므로 **git에 커밋**한다. 라우트를 추가/변경했다면 typecheck·커밋 전에 `vite build`(또는 `vite dev` 1회)를 먼저 실행해 이 파일을 최신 상태로 갱신해야 한다. 갱신을 빼먹어도 typecheck는 통과하지만(오래된 트리 기준으로만 검사) 새 라우트가 검증에서 누락된다.
- 디자인시스템은 `packages/ui`(shadcn+Tailwind v4)를 그대로 쓴다. `vite.config.ts`에 `@tailwindcss/vite` 플러그인이 등록되어 있고, `main.tsx`에서 `@ai-character-chat/ui/globals.css` 한 번만 import하면 전체 앱에 토큰/유틸리티가 적용된다(이 앱 자체는 별도 `tailwind.config`/전역 css를 갖지 않는다). 컴포넌트는 `@ai-character-chat/ui/components/{name}`에서 가져온다. 자세한 내용은 `packages/ui/CLAUDE.md` 참고.
- 모든 API 호출은 `shared/lib/api/client.ts`가 export하는 단일 `apiClient` axios 인스턴스를 거친다(직접 `axios.create` 금지). 인증은 httpOnly 세션 쿠키 + `withCredentials: true`로 처리되므로 요청 인터셉터에서 토큰을 붙이지 않는다. 응답 인터셉터가 모든 에러를 `@ai-character-chat/api-types`의 `ApiError`로 정규화해 reject하므로, 호출부의 `catch`/`onError`는 axios 원본 에러가 아니라 이 포맷을 받는다. 401 시 로그인 리다이렉트는 아직 미연결 — 로그인 라우트가 생기면 `setUnauthorizedHandler(() => router.navigate(...))`를 앱 초기화 시점(`app/providers.tsx` 등)에 한 번 등록한다.
- **`ApiError`는 FastAPI가 실제로 내려주는 `{"detail": ...}` 형태를 그대로 반영한다** (US-028에서 수정 — 이전 버전은 `{code, message, fields}`를 가정했는데 apps/api의 실제 에러 바디와 전혀 맞지 않았고, 그때까지 어떤 FE 코드도 실제 에러를 소비한 적이 없어 발견되지 않은 채로 있었다). `detail`이 string이면 HTTPException 메시지 그대로, dict면 구조화된 값(예: 429 응답의 `retryAfterSeconds`), 422 배열이면 `fields`로 필드명→메시지 매핑까지 펼쳐진다. 새 화면에서 서버 에러 메시지를 그대로 노출하기보다, `apiError.status`로 분기해 한국어 카피를 직접 작성하는 편이 일관된 UX를 준다(BE의 `detail` 문자열은 영어 디버그 메시지라 사용자 노출용이 아님).
- **`entities/session`**(`sessionKeys`, `useSessionQuery`)가 이 레이어의 첫 entity다 — `GET /me`를 `staleTime: Infinity`/`retry: false`로 캐싱한다(techspec-auth-onboarding.md §1). 로그인/회원가입/로그아웃 뮤테이션은 세션이 바뀔 때마다 `queryClient.invalidateQueries({queryKey: sessionKeys.current()})`만 호출하면 되고, 세션 갱신 로직을 직접 구현하지 않는다.
- **멀티스텝 위저드 폼(첫 사례: `features/sign-up`)은 스텝마다 별도 `useForm` 인스턴스를 쓰지 않고, 하나의 `useForm` + 하나의 zod 스키마를 전체 스텝에 걸쳐 공유**한다(techspec-overview.md §8). 스텝별 필드만 검증하려면 `form.trigger(['필드1', '필드2'])`로 해당 필드만 트리거한다 — zod resolver는 스키마 전체를 파싱하지만 RHF의 `trigger(names)`는 요청한 필드의 에러만 `formState.errors`에 반영하므로, 아직 도달하지 않은 다음 스텝의 필수 필드(예: 이메일 인증 코드) 때문에 현재 스텝 제출이 막히지 않는다. `form.handleSubmit()`은 스키마 전체를 검증하므로 스텝별 제출에는 쓰지 않는다.
- **shadcn `Checkbox`(`packages/ui`)는 네이티브 `<input>`이 아니라 Radix `checked`/`onCheckedChange` API**라 RHF `register()`로 못 묶는다 — `Controller`로 감싸거나(단일 필드), 두 필드를 동시에 다뤄야 하면(예: "전체 동의" 마스터 체크박스) `form.watch([...])`로 현재 값을 읽고 `form.setValue(name, value, { shouldValidate: true })`로 갱신하는 편이 중첩 `Controller`보다 간단하다.
- **(US-029) 라우터 컨텍스트에 `queryClient`가 실려있다**: `routes/__root.tsx`는 `createRootRoute` 대신 `createRootRouteWithContext<{ queryClient: QueryClient }>()`를 쓰고, `app/router.tsx`가 `createRouter({ routeTree, context: { queryClient } })`로 넘긴다(`queryClient`는 `app/providers.tsx`가 export하는 단일 인스턴스 — `QueryClientProvider`와 라우터 컨텍스트가 반드시 같은 인스턴스를 공유해야 캐시가 맞는다). **인증이 필요한 라우트는 `entities/session`의 `requireSession(queryClient, href)`를 `beforeLoad: ({ context, location }) => requireSession(context.queryClient, location.href)`로 호출**하면 된다(techspec-auth-onboarding.md §1) — 세션이 없으면(`GET /me` 401) `/login`으로 리다이렉트하고 원래 목적지를 `search.redirect`에 보존한다. `shared/lib/api/client.ts`의 `setUnauthorizedHandler`(US-005)는 이 용도로 쓰지 않는다 — 세션 체크 자체가 항상 401 없이 성공하는 게 아니라서(비로그인 방문자의 홈 화면 등) 전역으로 걸면 모든 게스트 방문이 로그인으로 튕기는 회귀가 생긴다. 이 훅은 "이미 로드된 화면에서 세션이 만료된 채 뮤테이션을 호출" 같은 별개 시나리오를 위해 미배선 상태로 남겨둔다.
- **세션 쿼리 옵션은 `entities/session/api/session-query-options.ts`의 `sessionQueryOptions`(`queryOptions()`로 생성) 하나를 `useSessionQuery`(컴포넌트용 `useQuery`)와 `requireSession`(라우터 가드용 `ensureQueryData`) 양쪽이 공유한다** — 쿼리키/함수/`staleTime`/`retry`가 어긋나면 캐시가 갈라지므로 새로 세션을 참조하는 코드는 이 옵션 객체를 그대로 재사용할 것.
- **(US-030) TanStack Router 플랫 파일명은 `.`으로 경로 세그먼트를 구분한다**: `routes/onboarding.google.tsx` → `/onboarding/google`. 새 라우트를 추가한 뒤에는 항상 `vite build`(또는 `vite dev` 1회)로 `routeTree.gen.ts`를 갱신할 것(위 커밋 정책과 동일).
- **(US-031) 라우트 진입 자체에 서버 검증 데이터가 필요하면 `beforeLoad`가 아니라 `loader`/`loaderDeps`를 쓴다**: `routes/reset-password.tsx`가 `loaderDeps: ({ search }) => ({ token: search.token })` + `loader: async ({ deps }) => ({ isTokenValid: await validatePasswordResetToken(deps.token) })`로 토큰을 검증한다. `requireSession`(beforeLoad, US-029)과 동일한 원칙을 따른다 — 실제 API 호출/에러 처리는 라우트 파일이 아니라 해당 `features/*`의 순수 async 함수에 두고, 라우트는 훅에 배선만 한다. 컴포넌트에서는 `Route.useLoaderData()`로 결과를 읽는다.
- **(US-030) 두 위저드가 완전히 같은 스텝 컴포넌트를 공유해야 하면(techspec이 "동일 컴포넌트 재사용"을 명시하는 경우) `features/*` 슬라이스 간 직접 import를 허용한다** — `features/onboarding-google`가 `features/sign-up`의 `GuardianConsentStep`/`useGuardianConsentMutation`/`SignUpFormValues`를 그대로 가져다 쓴다(이 저장소 첫 feature-to-feature import). 이 파일 최상단 레이어 규칙("역방향 import 금지")은 방향성만 금지할 뿐 동일 레이어 슬라이스 간 재사용까지 막지는 않는다 — 단, 재사용할 컴포넌트가 상대 feature에 구조적으로 결합되지 않고 폼 타입에 대해 완전히 제네릭일 때만 이 패턴을 쓸 것(그렇지 않다면 `entities/`로 끌어올릴 것). 새 `useForm` 인스턴스는 위저드마다 별도로 만들되(다른 페이지라 상태 공유가 불가능/불필요) 재사용 컴포넌트가 기대하는 타입은 그대로 import해 구조적 호환을 보장하고, jotai 스텝 atom도 위저드마다 새로 만들 것(모듈 전역 싱글턴이라 공유하면 라우트 간 스텝 상태가 샌다).
- **(US-033) 전역 `Header`는 `widgets/header`에 있고 `routes/__root.tsx`의 `RootComponent`(`<Header /><Outlet /></>`)에 한 번만 마운트된다** — 개별 라우트/페이지가 각자 헤더를 렌더링하지 않는다. 로그인 여부는 `useSessionQuery()`의 `data` 유무로 분기(`me`가 있으면 알림 벨+프로필 드롭다운, 없으면 로그인 버튼)하며, "로그인 사용자 전용" 하위 컴포넌트(`ProfileMenu` 등)는 `me: MeResponse`를 props로 받아 내부에서 세션을 다시 조회하지 않는다.
- **(US-033) 로그아웃처럼 "이 뮤테이션 성공 시 이미 마운트된 모든 컴포넌트가 세션 소실을 즉시 반영해야 하는" 경우엔 `queryClient.invalidateQueries`가 아니라 `resetQueries({ queryKey: sessionKeys.current() })`를 쓴다** — `invalidateQueries`는 스테일 표시 후 백그라운드 리페치가 끝날 때까지 기존(로그인) `data`를 그대로 유지해 헤더가 한 박자 늦게 바뀌고, `setQueryData(key, undefined)`는 TanStack Query가 "새 값이 `undefined`면 문서화된 no-op"으로 스킵해버려 아예 안 먹힌다. `resetQueries`만 살아있는 Query 객체의 `state.data`를 실제로 비우고 옵저버에 동기적으로 알린다(`features/logout/api/mutations.ts` 참고). 세션 관련 다른 뮤테이션(로그인/회원가입 등)은 기존 `invalidateQueries` 그대로 — 그쪽은 "없음→있음"이라 리페치 지연이 체감되지 않는다.
- **(US-033) 헤더의 프로필 드롭다운이 가리키는 라우트 중 아직 자기 스토리가 구현되지 않은 화면은 `shared/ui/ComingSoonPage.tsx`(제목/설명 props만 받는 범용 placeholder)로 임시 연결한다** — 빈 라우트나 404 대신 최소한의 안내 화면을 두어 헤더 자체는 완전한 상태로 유지하고, 실제 화면이 완성되는 스토리(예: 마이페이지=US-034, 프로필=US-035/036)에서 `ComingSoonPage` 호출부만 실제 컴포넌트로 교체하면 된다. 라우트 파일(`routes/{name}.tsx`)은 이미 그 스토리의 최종 형태(예: `beforeLoad: requireSession`)로 만들어두어 나중에 다시 손댈 필요가 없게 한다.
- **(US-034) 같은 ComingSoonPage 패턴을 마이페이지 밖에서도 쓴다**: 초안 카드가 이동하는 `/builder/$type/$draftId`(`routes/builder.$type.$draftId.tsx`, `pages/builder`)는 아직 빌더 스토리(US-100/US-106)가 없어 `ComingSoonPage`로 연결해뒀다 — 그 두 스토리에서 `pages/builder/ui/BuilderPage.tsx` 내부만 실제 빌더로 교체하면 되고, 라우트 파일(파라미터 파싱, `requireSession`)은 이미 최종 형태다.
- **(US-034) `DraftSummary`(`GET /me/drafts`)는 `thumbnailAssetId`(자산 UUID)만 주고 실제로 렌더링 가능한 URL을 주지 않는다** — 코드베이스 전체에 asset → 공개 URL로 변환하는 엔드포인트/유틸이 아직 하나도 없다(`apps/api`의 자산 API는 presigned PUT 업로드/완료 확인만 제공, GET/공개 read 경로 없음). `techspec-builder-common.md`의 `Draft` 예시 타입은 `thumbnailUrl`을 가정하지만 실제 구현은 다르다. 마이페이지 초안 카드는 그래서 이미지 없이 타입 아이콘(`lucide-react`의 `UserRound`/`BookOpen`)만 보여준다 — 홈/프로필/콘텐츠 상세처럼 실제 썸네일 이미지를 그려야 하는 다음 스토리(US-036 이후)에서 이 gap을 먼저 해결해야 한다(예: presigned GET 엔드포인트 추가, 또는 버킷을 공개 read로 설정하고 `storage_key`로 직접 URL을 구성).
- **(US-034) 파괴적 확인 모달의 첫 사례(회원탈퇴)는 `techspec-overview.md` §9가 지정한 `react-call`이 아니라 `packages/ui`에 새로 추가한 shadcn `alert-dialog`(Radix `AlertDialog`, 비제어 상태)를 그대로 썼다** — react-call은 이 저장소에 아직 의존성으로 없고, 단일 확인/취소 다이얼로그 하나에 새 라이브러리를 들이는 것보다 이미 있는 Radix 컴포넌트로 충분했다(`features/withdraw-account/ui/WithdrawAccountDialog.tsx`). `techspec-overview.md` §9 표에 나열된 다른 액션형 모달(신고 사유 선택=US-018, 삭제 확인 등)에 착수할 때 이 저장소에 react-call을 실제로 도입할지, 아니면 이 AlertDialog 패턴을 계속 재사용할지 먼저 판단할 것 — 지금은 둘 다 가능한 미확정 상태로 남겨둔다.
- **(US-035) 데이터 입력 폼 모달의 첫 사례(프로필 수정)도 `react-call` 없이 `packages/ui`의 평범한 shadcn `Dialog`(`open`/`onOpenChange`로 제어)를 그대로 썼다** — 비동기 저장 성공 시 프로그램적으로 닫아야 해서(단순 확인/취소가 아니라 "저장 중... → 성공 시 닫기") uncontrolled `DialogClose` 트리거만으로는 부족했다. `open` state를 열 때마다 `form.reset(defaultValues)`로 최신 서버 데이터로 되돌리는 것도 이 패턴에서 함께 처리한다(`features/edit-profile/ui/EditProfileDialog.tsx`의 `handleOpenChange` 참고). US-034의 react-call 미확정 메모와 마찬가지로, 다음 폼형 모달에서도 이 Dialog-직접-제어 패턴을 재사용할지 react-call을 들여올지는 그때 다시 판단한다.
- **(US-035) 자산 → 렌더링 가능한 URL 변환 갭(US-034가 남긴 이슈)은 BE가 해결했다**: `GET /users/{id}/profile`/`PATCH /me/profile`의 `UserProfileResponse`에 `profileImageUrl`(presigned GET URL, 매 조회마다 새로 서명됨 — 캐시하지 말 것)이 추가됐다. `DraftSummary`/`ContentSummary`의 `thumbnailAssetId`는 아직 이 필드가 없다 — 그 화면들이 실제 썸네일을 그려야 할 때 동일한 BE 패턴(`apps/api/CLAUDE.md`의 `generate_presigned_get_url` 참고)을 요청할 것.
- **(US-035) 첫 자산 업로드 플로우: `shared/lib/asset/uploadAsset.ts`의 `uploadAsset(file, purpose)`**가 presigned URL 발급(`POST /assets/presigned-upload`) → S3 직접 PUT(`fetch`, `apiClient` 아님 — 쿠키/baseURL이 없는 외부 절대 URL이라 인터셉터를 태우면 안 된다) → 완료 확인(`POST /assets/{id}/complete`) 3단계를 감싸고 최종 `assetId`를 반환한다. 다음에 이미지를 업로드하는 기능(상황별 이미지 등)은 이 유틸을 그대로 재사용할 것 — `purpose`만 새 `AssetPurpose` 값으로 바꾸면 된다.
- **(US-035) 라우트 동적 파라미터(`$userId`)도 검색 파라미터(US-031의 `loaderDeps`/`token`)와 동일한 원칙을 따른다**: 페이지 컴포넌트가 `routes/*.tsx`를 다시 import(`Route.useParams()`)하면 `routes → pages → routes` 순환 참조가 생기므로, 라우트 파일 안의 `RouteComponent`가 `Route.useParams()`로 값을 읽어 페이지에 **props로** 넘긴다(`routes/profile.$userId.tsx` 참고). `component: PageComponent`를 바로 넘기던 이전의 단순 라우트(파라미터가 없거나 페이지가 직접 `Route.useParams()`를 호출해도 순환이 없는 경우, 예: US-034 이전 라우트들)와 달리, 파라미터가 있고 페이지를 재사용/테스트 가능하게 두려면 이 wrapper 패턴이 필요하다.
- **(US-035) TanStack Query의 기본 `retry`(3회, 지수 백오프)는 4xx처럼 재시도해도 절대 성공하지 않는 에러에는 맞지 않는다** — `useProfileQuery`(`entities/profile`)는 `retry: (failureCount, error) => (error.status === 0 || error.status >= 500) && failureCount < 3`로 커스텀해 404(존재하지 않는 사용자) 같은 결정적 에러는 즉시 에러 상태로 넘어가게 했다(안 그러면 에러 UI가 뜨기까지 여러 초가 걸린다). 쿼리 결과의 `status`로 에러 화면을 분기하는 다음 read-쿼리도 이 패턴을 재사용할 것 — `QueryClient`(`app/providers.tsx`) 자체에는 아직 전역 기본값이 없다.
- **(US-036) `entities/content`가 처음 생겼다** — `model/types.ts`가 `techspec-content-versioning.md` §1의 `ContentVisibility`/`ModerationStatus`/`ContentAccessStatus`/`resolveAccessStatus`를 그대로 옮겨 담은 단일 진실 공급원이다. 상태 태그/접근 가드가 필요한 다음 화면(홈, 콘텐츠 상세, 채팅 라우트 가드)은 이 파일의 타입/함수를 재사용할 것 — 같은 계산을 다시 구현하지 않는다. `api/useProfileContentListQuery.ts`(`GET /users/{id}/contents`)는 `ContentSummary`도 함께 export한다.
- **(US-041) `entities/content`의 나머지 접근 가드(`canDiscoverPublicly`/`canAccessExistingRoom`/`canViewDetailPage`)도 `techspec-content-versioning.md` §1 그대로 추가 완료** — 라우트 로더/`beforeLoad`는 이 세 함수만 호출해 분기하고 가드 로직을 다시 구현하지 않는다(`canViewDetailPage`는 상세화면 신규 진입용, `canAccessExistingRoom`은 기존 대화방 재접속용으로 규칙이 다르니 섞어 쓰지 말 것).
- **(US-041, 첫 `apps/web` 자동화 테스트) vitest를 도입했다** — `apps/web/vitest.config.ts`(별도 파일, `vite.config.ts`와 분리 — 테스트가 React/TanStack Router/Tailwind 플러그인을 전혀 필요로 하지 않는 순수 로직이라 `environment: "node"`만 설정), `package.json`의 `test` 스크립트(`vitest run`), 루트 `turbo.json`/`package.json`에 `test` 태스크 추가(`pnpm run test`로 전체 워크스페이스 실행, 지금은 apps/web만 실제로 정의됨). `techspec-overview.md` §14가 명시한 대로 **UI 컴포넌트가 아니라 핵심 순수 로직에만** vitest 유닛테스트를 작성한다 — 테스트 파일은 대상 파일 옆에 `*.test.ts`로 둔다(`entities/content/model/types.test.ts` 참고). CI(`​.github/workflows/web.yml`)에도 typecheck 다음, build 이전 단계로 `turbo run test --filter=@ai-character-chat/web...`를 추가했다.
- **(US-036) 헤더 전역 `contentTypeToggleAtom`(`shared/model/content-type-toggle.ts`)과 프로필의 로컬 [스토리]/[캐릭터] 토글은 완전히 별개다** — 후자는 `routes/profile.$userId.tsx`의 `validateSearch`(zod, `type` optional, 기본값은 `RouteComponent`에서 구조분해 시 `= "character"`로 지정)로 관리하며, 변경 시 `Route.useNavigate()`(제네릭 `useNavigate()`가 아니라 라우트에 바인딩된 버전)로 `search`만 갱신한다. US-035의 "params는 RouteComponent가 읽어 페이지에 props로 넘긴다" 원칙을 search+변경 콜백까지 확장한 첫 사례 — 페이지(`pages/profile`)는 라우트 모듈을 몰라도 되고, 변경 요청은 콜백 prop으로만 위로 올린다. 공개여부 필터(전체/공개/링크공개/비공개)는 URL에 없는 로컬 `useState`다(techspec이 `type`만 URL 관리 대상으로 명시).
- **(US-036) `ContentSummary`(`GET /users/{id}/contents`)에 `thumbnailUrl`이 추가됐다** — US-035가 `UserProfileResponse.profileImageUrl`에 썼던 것과 동일한 BE 헬퍼를 재사용한다(이제 이름도 `_resolve_profile_image_url` → `_resolve_asset_url`로 일반화됨, `apps/api/src/api/content/router.py`). `DraftSummary`(`GET /me/drafts`)는 아직 이 필드가 없다 — 마이페이지 초안 카드가 실제 썸네일을 그려야 할 때 동일 패턴을 추가할 것.
- **(US-042) `entities/content/ui/ContentCard.tsx`는 `techspec-home-discovery.md` §3의 `ContentCardProps` 계약을 그대로 구현한 공용 카드다** — 아직 어떤 화면도 이 컴포넌트를 소비하지 않는다(홈=US-044, 즐겨찾기=미착수, 프로필은 US-036 당시 만든 로컬 `ContentCard`를 여전히 그대로 씀). 다음에 이 세 화면 중 하나에 착수하면 로컬로 다시 구현하지 말고 이 컴포넌트를 import할 것. `author`/`statusTag`는 optional(각각 "홈에서만"/"프로필 본인 조회 시에만" 노출이 기본 전제)이고, `thumbnailUrl`은 techspec 의사코드의 `string`이 아니라 실제 BE 응답과 동일하게 `string | null`로 구현했다. `author.profileUrl`은 타입에만 있고 아직 렌더링하지 않는다 — 카드 전체가 하나의 `<button>` 클릭 영역이라 작가명을 링크로 만들면 인터랙티브 요소가 중첩되므로, 실제로 클릭 가능하게 만들 화면이 나오면(아마 홈) `stopPropagation` 처리를 그때 추가할 것.
- **(US-042) 아직 소비 화면이 없는 공용 UI 컴포넌트를 브라우저로 검증할 때는, 기존 `/ui-demo`(US-003) 라우트에 임시로 데모 섹션을 추가해 chrome-devtools MCP로 확인한 뒤 그 페이지 변경분만 되돌린다**(US-029가 라우터 가드를 검증할 때 쓴 것과 같은 "임시 마운트 후 원복" 원칙, 첫 UI 컴포넌트 적용 사례) — 실제 커밋에는 새 컴포넌트 파일과 `index.ts` export만 남고, `/ui-demo` 자체는 원래 목적(packages/ui 프리미티브 데모)대로 깨끗하게 유지된다. 소비 화면이 이미 있는 컴포넌트는 그 화면에서 바로 검증하면 되므로 이 패턴이 필요 없다.
- **(US-043) TanStack Router의 `createBrowserHistory`는 `window.history.pushState`/`replaceState`를 감시용 래퍼로 덮어쓴다(인스턴스 프로퍼티 자체를 교체, `History.prototype`은 그대로 둠)** — techspec-content-detail.md §1이 가정한 "평범한 `window.history.pushState(...)`는 라우터가 모르는 조용한 URL 변경"이라는 전제가 이 버전(`@tanstack/react-router` 1.170)에서는 성립하지 않는다(실측: 그대로 호출하면 라우터가 즉시 재매치해 배경 리스트 페이지가 언마운트되고 풀페이지 라우트가 함께 렌더링됐다). 라우터 모르게 URL만 바꾸려면 `History.prototype.pushState.call(window.history, window.history.state, "", url)`처럼 프로토타입 메서드를 직접 호출해 인스턴스 래퍼를 우회할 것(`shared/lib/hooks/useContentDetailModal.ts`) — `window.history.back()`/`forward()`/`go()`는 패치 대상이 아니므로 그대로 써도 된다. 모달/URL 동기화가 필요한 다음 기능도 이 우회를 재사용할 것.
- **(US-043) `entities/content`가 첫 상세 조회 쿼리(`useContentDetailQuery`, `GET /contents/{id}`)를 얻었다** — 응답의 `accessStatus`는 BE가 평평하게(`{kind, visibility?}`) 내려주므로, `entities/content`의 판별 유니언 `ContentAccessStatus`로 쓰려면 먼저 `toContentAccessStatus()`로 변환해야 한다(직접 `canViewDetailPage`에 넘기면 타입 에러). `widgets/content-detail/ui/ContentDetailView.tsx`가 모달/풀페이지 공용 실제 콘텐츠이고, 상위 레이어(`pages/content-detail`, `widgets/content-detail/ui/ContentDetailModalOutlet.tsx`)는 그 결과를 각각 풀페이지 레이아웃/Radix `Dialog`로 감싸기만 한다. 카드가 있는 리스트(첫 사례: 프로필의 `ProfileContentSection`)는 `useContentDetailModal().open(type, id)`만 호출하면 되고, 이 뷰를 직접 import하지 않는다.
- **(US-043) Radix `DialogContent`는 `DialogTitle`이 없으면 a11y 경고를 낸다** — `ContentDetailModalOutlet`이 실제 제목(`ContentDetailView` 내부의 평범한 `<h1>`, 모달/풀페이지 양쪽에서 재사용)과 별개로 `<DialogTitle className="sr-only">콘텐츠 상세정보</DialogTitle>`를 추가로 렌더링한다 — `ContentDetailView`를 Radix `DialogTitle`에 결합시키지 않기 위함(풀페이지 컨텍스트에는 Dialog 자체가 없으므로 그 안에서 Radix Title을 쓰면 에러난다).
- **(US-044) 첫 무한스크롤 리스트**: `shared/lib/hooks/useInfiniteScrollSentinel.ts`(IntersectionObserver로 sentinel div가 보이면 콜백 호출)를 `useInfiniteQuery`의 `fetchNextPage`와 조합한다(`pages/home/ui/HomePage.tsx`). 정렬/필터 변경으로 `useInfiniteQuery`의 queryKey가 바뀌면 그 자체로 새 쿼리가 되어 `isPending`이 다시 true가 되므로("US-007 AC의 로딩 표시"), 별도 로컬 로딩 상태 없이 기존 `isPending` 스켈레톤 패턴을 그대로 재사용하면 된다. 즐겨찾기(techspec-home-discovery.md §4)도 같은 훅+패턴을 재사용할 것.
- **(US-044) `ContentCard`(`entities/content`)의 바깥 컨테이너를 `<button>`에서 `role="button"` `div`(`tabIndex=0`+`onKeyDown`으로 Enter/Space 처리)로 바꿨다** — `onAuthorClick`이 주어지면 작가명이 별도 `<button>`이 되는데, `<button>` 안에 `<button>`을 중첩하는 건 유효하지 않은 HTML(브라우저가 파서 단계에서 트리를 깨뜨림)이라 바깥 요소를 바꿔야 했다. 카드 안에 클릭 가능한 하위 요소를 추가로 넣어야 하는 다음 케이스도 이 패턴(바깥은 `role="button"` div, 안쪽만 진짜 `<button>` + `stopPropagation`)을 따를 것.
- **(US-044) 홈의 크리에이터/해시태그 필터는 전용 UI를 새로 만들지 않고, 이미 존재하는 화면의 클릭 가능한 지점에 필터 진입점을 얹었다**: `ContentCard`의 작가명 클릭(`onAuthorClick`, US-042가 남긴 breadcrumb)은 홈 검색결과를 그 작가로 좁히고, `widgets/content-detail/ui/ContentDetailView.tsx`의 해시태그(기존엔 평범한 `<span>`)를 `<button>`으로 바꿔 클릭 시 홈으로 이동하며 `?hashtag=`를 채운다. 둘 다 홈 라우트의 search param(`creator`/`hashtag`)을 채우는 것만 하고, 필터 해제는 홈 화면에 노출되는 작은 "활성 필터" 칩(✕ 버튼)으로 처리한다 — 장르처럼 항상 보이는 선택 UI가 없는 필터 차원은 이렇게 "칩에서 진입 + 홈 상단 활성 필터 칩에서 해제"로 다루는 게 새 드롭다운/모달을 만드는 것보다 단순했다.
- **(US-044) 여러 search param을 독립적으로 관리하는 라우트에서 `navigate({ search: {...} })`처럼 객체 리터럴을 통째로 넘기면 다른 필드가 전부 날아간다** — `widgets/header/ui/SearchInlineExpand.tsx`가 원래 `search: { q }`로 검색어만 넘기고 있었는데, 홈에 `sort`/`genre`/`creator`/`hashtag`가 추가되면서 검색어를 입력할 때마다 그 필터들이 초기화되는 버그가 됐다(US-044 전에는 `q`가 유일한 홈 search 필드라 드러나지 않았음). `search: (prev) => ({ ...prev, q: ... })` 형태(함수형 업데이터로 병행 갱신)로 고쳤다 — 앞으로 홈 search 스키마에 필드를 추가할 때마다, 그 필드를 갱신하는 모든 호출부가 객체 리터럴이 아니라 함수형 업데이터를 쓰는지 확인할 것.
- **(US-045, 첫 진짜 낙관적 업데이트+디바운스 뮤테이션) 좋아요 토글은 TanStack Query의 `onMutate`/캐시 조작을 전혀 쓰지 않는다** — 대신 로컬 `useState<boolean | undefined>`(`desiredLiked`, 서버 값과 다를 때만 값이 들어있는 "의도" 오버라이드)로 화면 표시(`isLiked`/`likeCount`)를 서버 데이터 위에 얹어 매 클릭마다 즉시 반영하고, `react-use`의 `useDebounce(..., 400, [desiredLiked])`(US-044의 `SearchInlineExpand`와 같은 훅)로 실제 네트워크 호출(`useToggleLikeMutation`)만 지연시킨다. 성공/실패 모두 `onSettled`에서 `desiredLiked`를 리셋(단, 그 사이 연타로 값이 또 바뀌었으면 덮어쓰지 않도록 `current === syncingLiked`일 때만 리셋)하고 상세 쿼리를 무효화한다 — 실패 시 로컬 오버라이드를 지우면 화면이 자동으로 서버값(변경된 적 없음)으로 돌아가므로 별도 롤백 코드가 필요 없다. 좋아요/즐겨찾기 등 다음 낙관적 토글도 이 모양(로컬 override state + `useDebounce`로 네트워크만 지연 + `onSettled` 조건부 리셋)을 재사용할 것 — 캐시 레벨 `onMutate`보다 단순하고, 이 화면 하나만 읽는 값이라 캐시 오염 위험도 없다.
- **토글 클릭 핸들러에서 렌더 클로저의 파생값을 읽어 `setState(!derived)`처럼 리터럴 값을 넘기면, React가 같은 틱에서 여러 클릭을 배칭할 때(예: 매크로 없는 프로그램적 연속 클릭, 혹은 동일 이벤트 루프 틱 안에서 발생하는 중복 디스패치) 매 클릭이 전부 같은 스테일 값으로 계산돼 "짝수/홀수 클릭 수와 무관하게 마지막 클릭의 값으로 고정"되는 버그가 난다**(`ContentDetailView`의 좋아요 버튼에서 실측 — 4연타가 항상 같은 결과로 수렴했다). `setDesiredLiked((current) => !(current ?? content.isLiked))`처럼 항상 함수형 업데이터로 이전 상태에서 파생시킬 것 — 렌더 시점 변수를 클로저로 캡처하지 않으므로 배칭 여부와 무관하게 항상 정확하다. "연타 시에도 상태가 일관되게 유지"가 AC에 있는 토글은 전부 이 패턴을 기본값으로 쓸 것.
- **(US-046) 대화방 생성 API(US-051 캐릭터 챗/US-057 스토리 챗)가 아직 없어, 플레이 버튼(`widgets/content-detail/ui/{StoryDetailBody,CharacterPlayButton}.tsx`)은 `/chat/$type/$id`(`routes/chat.$type.$id.tsx`, `requireSession` 가드 + `ComingSoonPage`)로 이동하는 것까지만 책임진다** — US-034 빌더 스텁과 같은 패턴. `widgets/content-detail/lib/usePlayContent.ts`의 `start()` 내부 navigate 대상만 나중에 실제 방 생성 뮤테이션으로 바꾸면 된다.
- **(US-046) `techspec-content-detail.md` §3의 `useEffectOnce(() => { if (restoredSetupId && session.data) ... })` 의사코드를 그대로 구현하면 로그인 복귀 자동 플레이가 거의 항상 실패한다** — `useEffectOnce`(`react-use`)는 마운트 시점 단 한 번만 실행되는데, `useSessionQuery()`의 `GET /me`는 비동기라 최초 렌더 시점엔 `session.data`가 아직 `undefined`인 게 보통이라 그 순간의 체크가 영구히 스킵된다. `usePlayContent`는 대신 일반 `useEffect(() => {...}, [session.isPending, session.data])` + `useRef` 가드(한 번 실행되면 다시 안 하도록)로 구현해, `session.isPending`이 꺼질 때까지 기다렸다가 정확히 한 번만 판단한다.
- **(US-046) `navigate({ to: "/login", search: { redirect: "<pathname>?<query>" } })`처럼 `redirect` 검색 파라미터 *값 자체*에 물음표(`?`)가 포함된 문자열을 넣어도 안전하다** — TanStack Router가 그 값을 다른 검색 파라미터와 마찬가지로 통째로 percent-encode하므로 실제 URL은 `?redirect=%2Fcontent%2Fstory%2F...%3FautoplayId%3D1...`처럼 인코딩되고, `LoginForm`의 `navigate({ to: redirectTo })`(디코딩된 원본 문자열)가 그 전체 경로+쿼리를 다시 정상적으로 라우트 매치한다(브라우저로 왕복 확인). `requireSession`이 넘기는 `location.href`도 같은 방식이라 쿼리가 있는 보호 라우트 재진입도 이미 안전했다.
- **(US-047) shadcn `Dialog`를 다른 `Dialog` 안에 중첩해도(첫 사례: `VersionHistoryModal`을 `ContentDetailView` 안에서 열되, 그 `ContentDetailView` 자체가 이미 `ContentDetailModalOutlet`의 `Dialog` 안에 렌더링된 상태) 추가 배선 없이 그대로 동작한다** — Radix `DialogPortal`이 각자 별도 포털/오버레이를 렌더링해 안쪽 Dialog가 바깥 Dialog 위에 올바르게 스택되고, `Escape`는 가장 위(안쪽) Dialog만 닫으며 바깥 Dialog는 열린 채로 남는다(브라우저로 두 번의 `Escape`를 눌러 각각 확인). 상세화면 안에서 또 다른 모달을 열어야 하는 다음 케이스(예: US-048 신고 사유 선택 모달)도 이 조합을 그대로 재사용할 수 있다.
- **(US-048) `react-call`을 이 저장소에 처음 도입했다** — US-034/035/047이 남겨둔 미확정 메모("액션형 모달을 react-call로 갈지 기존 Dialog 직접제어로 갈지")를 이 스토리에서 확정: `techspec-overview.md` §9가 신고 사유 선택(US-018)을 명시적으로 react-call 사례로 지정했고, 앞으로도 시작설정 변경 확인(US-027)/삭제 확인/대화방 삭제·초기화 확인 등 같은 종류의 "열고 → 입력/확인 → 결과 반환 후 종료" 모달이 반복될 예정이라 지금 도입하는 것이 맞다고 판단했다(주의: US-049는 삭제 확인이 아니라 즐겨찾기 기능으로 확정됐다 — 이 스토리에 붙어있던 예상 ID는 틀렸다). `features/report-content/ui/ReportContentModal.tsx`(`createCallable<Props, void>`, `react-call/mutation-flow`의 `useMutationFlow`)가 첫 사례다. 실제 API 호출/토스트/성공 판단은 이 컴포넌트가 스스로 하지 않고 **호출부가 넘기는 `mutationFn`이 담당**한다(`ContentActionsMenu.handleReport`가 `ReportContentModal.call({ mutationFn: async (call, payload) => { try { await mutate(...); call.end() } catch { /* end() 호출 안 함 → 다이얼로그가 열린 채로 남아 재시도 가능 */ } } })` 형태) — 이 컴포넌트는 오직 입력 UI + `submit.pending` 표시만 책임진다. Callable 자체(`<ReportContentModal />`)는 다른 지속 마운트 오버레이(`ContentDetailModalOutlet`)와 나란히 `routes/__root.tsx`에 한 번만 마운트한다(react-call 문서의 "Root"가 바로 이 컴포넌트 자신이다 — 별도 `<Root>` 래퍼는 없음). shadcn `Dialog`로 감쌀 때는 `open={!call.ended}` + `onOpenChange={(open) => !open && call.end()}`로 배선해 ESC/배경클릭도 정상적으로 `call.end()`를 태우게 한다.
- **(US-048) shadcn `ToggleGroup`을 단일선택 UI로 쓸 때 로컬 state의 초기값을 `undefined`로 두면 안 된다** — Radix가 "처음엔 uncontrolled, 선택 후엔 controlled"로 전환되는 것으로 인식해 콘솔에 controlled/uncontrolled 전환 경고를 낸다(`StoryDetailBody`처럼 초기값이 항상 존재하는 케이스에선 드러나지 않았던 문제, `ReportContentModal`처럼 "아무것도 선택 안 된 초기 상태"가 유효한 케이스에서 처음 발견). `value={state ?? ""}`로 항상 정의된 값을 넘겨 처음부터 controlled로 유지할 것 — 빈 문자열은 어떤 `ToggleGroupItem`의 `value`와도 매치되지 않으므로 "선택 없음"이 그대로 표현된다.
- **(US-048) 로컬 dev 환경에서 Redis 세션 쿠키가 Postgres보다 오래 산다** — 브라우저 프로필의 `session_id` 쿠키는 이전 스토리 검증 세션에서 만들어진 채로 남아있는데, 그 사이 로컬 Postgres가 재시딩/초기화되어 그 세션이 가리키던 `user_id`가 더 이상 `users` 테이블에 없는 상태가 될 수 있다. 이 상태로 로그인 필요 뮤테이션(좋아요/즐겨찾기/신고 등, `user_id`에 FK가 걸린 모든 테이블)을 호출하면 BE가 처리 중 처리되지 않은 `IntegrityError`(FK 위반)로 500을 내고, 이걸 브라우저는 "CORS policy" 에러(`net::ERR_FAILED`)로 오표시한다 — 실제로는 CORS와 무관하다(프리플라이트 OPTIONS는 정상 200이었음, `uvicorn` 로그의 스택트레이스로 진짜 원인 확인). 브라우저 검증 중 로그인 관련 액션이 뜬금없이 CORS 에러로 실패하면 CORS 설정을 의심하기 전에 먼저 `/dev/session-echo`로 방금 시딩한 실제 `user_id`를 담은 새 세션을 발급받아 쿠키를 갱신할 것.
- **(US-052) `entities/chat-room`이 처음 생겼다** — 캐릭터/스토리 챗이 공유하는 `ChatMessage`/`ChatRoomState` 타입, `chatRoomKeys`, `applyStreamEvent`(SSE 이벤트→Query 캐시 반영 순수 함수, techspec-chat-story.md §1.2가 유일한 정의처), `buildSendPayload`가 있다. `ChatRoomState.contentSnapshot`이 참조하는 `StatDef`/`Shortcut`/`Ending`(+ 규칙 트리 `SingleRule`/`RuleGroup`/`RuleListItem`)은 스토리 빌더(US-091~114, 아직 미구현)가 정의할 zod 스키마(techspec-builder-story.md §1.2/§1.4/§1.5)를 일반 TS 타입으로 앞당겨 옮긴 것 — 빌더 스토리에서 실제 zod 스키마를 만들 때 이 타입들과 어긋나지 않는지 대조할 것. `applyStreamEvent`/`buildSendPayload`는 훅이 아니라 `QueryClient`를 인자로 받는 순수 함수라, 테스트에 React Testing Library 없이 `new QueryClient()` + `setQueryData`/`getQueryData`만으로 충분하다(entities/chat-room/model/applyStreamEvent.test.ts 참고) — 캐시를 직접 조작하는 다음 순수 함수도 이 패턴을 재사용할 것. 실제 SSE 연결/전송 훅(US-053/054)과 소비 화면(US-055)은 아직 없다.
- **(US-054) 첫 fetch 기반 SSE 소비 유틸 — `shared/lib/sse/openChatStream.ts`**: 메시지 전송은 본문 있는 POST라 네이티브 `EventSource`(GET 전용)를 못 쓴다. `fetch(..., { credentials: "include" })`(axios `apiClient`가 아니다 — axios는 스트리밍 응답 바디를 `ReadableStream`으로 넘겨주지 않는다, 인증은 `withCredentials`와 동일하게 `credentials: "include"`로 직접 챙긴다) → `response.body.pipeThrough(new TextDecoderStream()).getReader()`로 청크를 읽고 `\n\n`으로 이벤트를, `data:` 접두사 줄만 모아 `JSON.parse`한다(BE의 `EventSourceResponse`가 내는 프레임과 대응, apps/api/CLAUDE.md의 SSE 항목 참고). `apiClient.ts`가 이제 `apiBaseUrl`도 export한다(axios `baseURL`과 fetch 둘 다 같은 값을 쓰기 위함). 이 함수는 `TEvent` 제네릭이라 이벤트 타입(`ChatStreamEvent`)을 모르며, 소비 측(`entities/chat-room`)이 타입 인자로 채운다.
- **(US-054) `features/send-message/model/useSendMessage.ts`**: 전송 즉시 사용자 메시지를 `chatRoomKeys.detail(roomId)` 캐시에 낙관적으로 추가 → `openChatStream`을 순회하며 매 이벤트를 `applyStreamEvent`에 그대로 위임(캐시 반영은 전부 그쪽 책임) → `token`/`policyWarning`/`error`만 이 훅이 별도로 로컬 state(`streamingText`/`policyWarning`/`error`)로 관리한다(techspec-overview.md §5: 스트리밍 중 텍스트는 로컬 버퍼, 스트림 종료 시 비움 — Query 캐시와 이중 상태 금지). `retry()`는 `error.retryPayload`(직전 `buildSendPayload` 결과)로 `openStream`만 다시 부르고 `send()`(따라서 낙관적 메시지 추가)는 절대 다시 호출하지 않는다 — 이게 "재시도 시 사용자 메시지 중복 추가 금지"를 만족시키는 방법이다. 소비 화면(US-055)이 아직 없다 — 다음 스토리는 이 훅을 `/chat/$roomId` 화면에 배선하면 된다.
- **(US-055) `ChatRoomState`가 BE의 실제 `ChatRoomResponse`(US-051, 캐릭터 챗)와 맞지 않던 부분을 이 스토리에서 처음으로 실사용하며 바로잡았다** — techspec-chat-story.md §1.1의 원래 필드명은 `storyId`였지만 실제 응답은 `contentId`/`contentType`/`name`을 쓴다(캐릭터 콘텐츠에 `storyId`라는 이름을 쓰는 것 자체가 어색했던 스토리-챗-먼저 가정의 잔재). `entities/chat-room/api/types.ts`에서 `storyId` → `contentId`로 rename하고 `contentType`/`name`을 추가했다. `startingSetupId`/`contentVersion`/`contentSnapshot`은 스토리 챗 전용이라 BE가 아직 안 보내므로(US-057 이전) optional로 바꿨다 — `stats`/`endingStatus`는 techspec-chat-character.md §0이 "캐릭터 챗은 항상 빈 값"이라고 명시적으로 확정해뒀으므로 그대로 required 유지. 새 순수 함수 `entities/chat-room/model/toChatRoomState.ts`(BE DTO → `ChatRoomState` 매핑, `useChatRoomQuery`/`useStartChatMutation` 양쪽이 재사용)가 이 경계를 담당한다 — `applyStreamEvent`가 이미 같은 캐시 키를 `ChatRoomState` 모양으로 다루고 있어서, 최초 조회도 반드시 이 모양으로 저장해야 SSE 이벤트가 이어서 반영된다.
- **(US-055) `useContentDetailQuery(id, enabled = true)`에 `enabled` 파라미터가 추가됐다**(기존 호출부는 전부 영향 없음, 새 두 번째 인자를 안 넘기면 기존과 동일) — `useContentVersionsQuery`가 이미 쓰던 것과 같은 패턴. 챗 화면(`widgets/chat-room/ui/ChatRoomView.tsx`)처럼 id를 room 쿼리 응답이 도착해야만 알 수 있는 "의존 쿼리" 상황에서 재사용할 것.
- **(US-055, 첫 실제 대화 화면) `usePlayContent`의 `start()`가 콘텐츠 타입별로 갈라진다** — 캐릭터는 이제 `useStartChatMutation`(`entities/chat-room`, `POST /chat-rooms`)으로 실제 방을 만들고 `/chat/$roomId`로 이동한다(스텁 시대 종료). 스토리는 BE가 아직 `contentType: "story"`를 받지 않아(`ChatRoomCreateRequest.contentType`이 `Literal["character"]`) 여전히 `/chat/$type/$id`(ComingSoonPage 스텁, `routes/chat.$type.$id.tsx`)로 간다 — US-057이 BE에 스토리 챗 방 생성을 추가하면 이 분기를 없애고 스토리도 `useStartChatMutation`을 확장해서 쓰면 된다.
- **(US-055) SSE 스트리밍/에러/정책경고 3가지 분기를 전부 브라우저로 실측하려면 BE 코드를 일시적으로 손대야 했다** — 실제 `GEMINI_API_KEY` 없이는 정상 토큰 스트림도, 정책위반도 재현할 수 없다(정상 실패는 `LLMClientError`로 잡혀 이미 error 배너 검증엔 충분했지만, 토큰별 실시간 반영과 policyWarning은 별도로 강제해야 확인 가능). `apps/api/src/api/chat/router.py`의 `send_message` 제너레이터 안 `llm_client.generate(prompt)` 호출부를 임시로 (a) 고정 딜레이를 둔 하드코딩 토큰 목록으로, (b) `raise LLMPolicyViolationError(...)`로 각각 바꿔 재현한 뒤 `git checkout -- apps/api/src/api/chat/router.py`로 원복했다 — 다음에 SSE 관련 화면을 검증할 때도 이 방법(임시 코드 수정 → 브라우저 확인 → `git checkout --`로 완전 원복, 커밋 전 `git status`로 깨끗한지 재확인)을 재사용할 것.
- **(US-056) "내 채팅목록"(`/chats`)은 헤더 드롭다운 항목이지만 실제로는 콘텐츠(캐릭터/스토리) 단위로 스코프된 화면이다** — BE의 `GET /chat-rooms`가 `contentId` 없이는 조회 자체가 불가능하고(같은 콘텐츠로 여러 대화방을 관리하는 기능, 전체 콘텐츠를 가로지르는 통합 인박스가 아님), techspec-chat-common.md §3도 `useChatRoomListQuery({contentId, contentType})`로 명시한다. `routes/chats.tsx`의 `contentId`/`contentType` search param을 **optional**로 두어(둘 다 zod `.optional()`) 콘텐츠 상세(`widgets/content-detail/ui/CharacterPlayButton.tsx`)의 새 "내 대화 목록" 진입점(로그인 시에만 노출, `/chats?contentId=&contentType=character`)에서는 실제 목록을, US-033부터 있던 헤더의 파라미터 없는 링크에서는 안내 문구만 보여준다(`pages/chats/ui/ChatsPage.tsx`) — 헤더 링크를 깨뜨리지 않으면서 실제 스펙을 만족시키는 절충. 스토리 콘텐츠에는 이 진입점을 아직 안 달았다(`POST /chat-rooms`가 `contentType: "character"` literal만 받아 US-057 전까지 스토리 방 자체가 생성될 수 없으므로 목록이 항상 빈 상태 — US-057에서 `StoryDetailBody.tsx`에도 같은 링크를 추가할 것).
- **(US-056) 입력 없이 확인/취소만 있는 react-call 모달은 공용 컴포넌트 하나로 공유한다** — `features/manage-chat-room/ui/ConfirmChatRoomActionModal.tsx`(`createCallable<Props, void>`, `MutationFn<void>`)가 title/description/confirmLabel을 props로 받아 초기화·삭제 두 액션 모두에 재사용된다. US-048의 `ReportContentModal`(입력 있는 사유 선택형)과 달리 이 모달은 입력 UI가 없어 두 호출부가 완전히 같은 마크업을 필요로 했다 — 다음에 또 "확인/취소뿐인" 액션 모달(예: 시작설정 변경 확인)이 필요하면 새 컴포넌트를 만들지 말고 이걸 재사용할 것. 입력 UI가 있는 확인 모달은 계속 `ReportContentModal`처럼 전용 컴포넌트로 만든다.
- **(US-056) 대화방 초기화는 `useResetChatRoomMutation`(`entities/chat-room`) 내부에서 자체적으로 `onSuccess`에 `queryClient.setQueryData(chatRoomKeys.detail(roomId), room)`을 호출해 "캐시 전체 교체"를 보장한다** — 호출부(리스트 화면)가 캐시 처리를 잊어도 항상 지켜지도록 훅 안에 넣었다(다른 토글류 뮤테이션처럼 호출부가 캐시 처리를 결정하는 패턴과 다름, 이 뮤테이션은 "서버 응답으로 detail 캐시를 통째로 교체"가 유일하게 옳은 동작이라 훅에 고정). 목록 화면의 `lastMessagePreview` 갱신은 별도로 `chatRoomKeys.list(...)` invalidate로 처리한다(초기화/삭제 모두 호출부가 담당) — 두 캐시(list/detail)는 독립적으로 무효화해야 한다.
- **(US-060) 스토리 챗 FE 배선이 완성되면서 캐릭터/스토리 분기가 사라졌다** — `usePlayContent.start()`는 이제 콘텐츠 타입 분기 없이 항상 `useStartChatMutation({ contentId, contentType, startingSetupId })` → `/chat/$roomId`로 이동한다(`startingSetupId`는 캐릭터일 땐 `undefined`, BE `ChatRoomCreateRequest.startingSetupId`가 optional이라 문제없다). 이 변경으로 `/chat/$type/$id`(`routes/chat.$type.$id.tsx`)와 그 `pages/chat/ui/ChatPage.tsx`(ComingSoonPage 스텁)가 완전히 고아가 되어 삭제했다 — `ChatRoomView.tsx` 하나가 캐릭터/스토리 공용 대화 화면이다.
- **(US-060) `entities/chat-room/model/toChatRoomState.ts`가 BE의 스토리 전용 필드(`startingSetupId`/`stats`/`contentSnapshot`)를 처음으로 실제 매핑한다** — BE는 DB 컬럼명 그대로의 raw 값(`StatDefSnapshot.minValue`/`maxValue`/`initialValue`, `EndingRuleItem.operator`의 `gte`/`lte`/`eq`/`gt`/`lt`)을 쓰고 FE의 provisional 타입(US-052, techspec 의사코드 기준)은 `min`/`max`/`initial`과 비교기호(`>=` 등)를 쓴다 — 이 경계 파일이 그 변환(`OPERATOR_MAP` 등)을 전담한다. 캐릭터 챗은 BE가 이 필드들을 안 보내(`null`/`undefined`) 기존처럼 빈 값으로 유지된다.
- **(US-060) `widgets/chat-room/ui/StatGaugePanel.tsx`가 `room.contentSnapshot.stats`(정의)와 `room.stats`(현재값, statId로 조인)로 게이지를 그린다** — `ChatRoomView`는 `room.contentSnapshot`이 있을 때만(즉 스토리 챗일 때만) 헤더 아래에 이 패널을 렌더링한다(캐릭터 챗은 `contentSnapshot`이 항상 `undefined`). `statChange` SSE 이벤트는 이미 `applyStreamEvent`(US-052)가 같은 캐시 키에 반영하므로 게이지는 별도 배선 없이 자동으로 리렌더링된다. 트랙 바 채우기 색상은 `stat.color`(자유 텍스트, 아직 빌더가 없어 포맷 미확정)를 인라인 `style`로 그대로 적용한다 — packages/ui에 아직 Progress 프리미티브가 없어 커스텀 div 바로 구현했다.
- **(US-063) `endingReached` SSE의 `epilogue`는 `ChatRoomState.endingStatus`에 `epilogue: string | null` 필드로 함께 저장한다(세션 한정, best-effort)** — BE의 `GET /chat-rooms/{id}`는 `ending_reached: bool`만 돌려주고 `endingId`/`epilogue`는 안 보낸다(US-057부터 그랬음, `endingId`/`reachedAtTurn`도 이미 같은 처지였다). 그래서 새로고침/포커스 리페치가 일어나면 이 세 필드는 `null`로 되돌아간다 — 의도된 동작이다: "어떤 엔딩에 도달했는지 나중에도 다시 확인"은 별도 기능(엔딩 컬렉션, US-069/070)이 담당할 몫이라 이 스토리에서 BE 응답을 확장하지 않았다. `widgets/chat-room/ui/ChatRoomView.tsx`는 `room.endingStatus.reached && room.endingStatus.epilogue`일 때만(둘 다 있을 때만) `MessageBubble.tsx`의 `EndingDivider`(구분선+배지, `contentSnapshot.endings`에서 `endingId`로 이름을 조인)와 에필로그 말풍선(일반 `MessageBubble` 재사용)을 `room.messages` 뒤에 추가로 렌더링한다 — `applyStreamEvent`가 갱신하는 `endingStatus`와 `messages`가 서로 다른 필드라 순서가 꼬이지 않는다(엔딩 이벤트는 `done`보다 먼저 오지만, 렌더링은 항상 메시지 목록 다음에 붙는다).
- **(US-063이 발견한 기존 버그, 아직 미수정)** 홈/프로필의 카드 클릭으로 연 콘텐츠 상세 **모달**(`useContentDetailModal`, jotai `contentDetailModalAtom`)에서 "플레이"를 눌러 `usePlayContent.start()`가 `navigate({ to: "/chat/$roomId" })`로 진짜 라우터 이동을 해도, 그 네비게이션은 라우터가 매치하는 route만 바꿀 뿐 `contentDetailModalAtom`은 건드리지 않는다 — 모달이 새 `/chat/$roomId` 화면 위에 그대로 뜬 채로 남는다(`ContentDetailModalOutlet`은 이 atom만 보고 열림 여부를 결정하는 라우트-비의존 전역 상태라서). 재현: 홈에서 스토리 카드 클릭(모달로 상세 열림) → "플레이" 클릭. 직접 `/content/$type/$id` 전체페이지로 진입해 플레이하면 애초에 모달 상태가 없어 재현되지 않는다 — 지금까지의 브라우저 검증들이 이 경로를 우연히 피해갔을 가능성이 높다. 고치려면 `usePlayContent.start()`(또는 그 호출부)가 네비게이션 직전에 `useContentDetailModal().close()`(혹은 `setState(null)`)를 호출해야 한다 — 이번 스토리 범위 밖이라 수정하지 않았다.
- **(US-066) `packages/ui`에 Popover/Command가 없는 상태에서 텍스트 입력에 붙는 자동완성 드롭다운이 필요하면, 새 프리미티브를 들이지 말고 평범한 `relative` 래퍼 + 조건부 렌더링 `absolute` div로 충분하다** — `features/shortcut-autocomplete/ui/ShortcutAutocomplete.tsx`가 첫 사례(`widgets/chat-room/ui/ChatRoomView.tsx`가 `Textarea`를 `relative` div로 감싸고, 텍스트가 `/`로 시작할 때만 그 안에 `absolute inset-x-0 bottom-full mb-2` 드롭다운을 마운트). 이 footer 영역은 메시지 리스트(`overflow-y-auto`)와 분리된 별도 `shrink-0` 블록이라 클리핑 걱정이 없었다 — 스크롤 컨테이너 **안에** 이런 드롭다운을 붙여야 하면 `position: fixed`나 포털이 필요하다(impeccable 가이드). 배경/그림자는 새로 만들지 않고 기존 `select.tsx`/`dropdown-menu.tsx`가 쓰는 `bg-popover text-popover-foreground shadow-md ring-1 ring-foreground/10`을 그대로 재사용했고, 항목 hover/focus는 `select.tsx`의 `SelectItem`과 동일하게 `hover:bg-accent`(Radix 아님, 평범한 `<button>`)로 맞췄다. 진입 모션은 Radix 컴포넌트의 `data-open:animate-in`이 아니라(그 attribute 자체가 없음) 이 저장소의 손수 만든-엘리먼트 컨벤션(`motion-safe:animate-in motion-safe:fade-in-0 motion-safe:zoom-in-95`, `features/sign-up`의 스텝 트랜지션과 동일 패턴)을 썼다.
- **(US-066) 필 형태의 클릭형 칩(추천답변 등)은 새 컴포넌트를 안 만들고 기존 `Button`에 `variant="secondary" size="sm" className="rounded-full"`만 얹으면 된다** — hover/focus/disabled 상태가 전부 공짜로 따라온다("Same button shape" product 원칙). `rounded-full bg-secondary text-secondary-foreground`로 손수 마크업했던 기존 뱃지류(`ProfileContentSection` 등)는 비클릭 상태 표시 전용이니 혼동하지 말 것 — 클릭 가능한 칩은 항상 `Button` variant를 재사용한다.
- **(US-068, 첫 shadcn `Sheet` 도입) "목록 탐색"(패널)과 "기능 실행/조회"(모달)의 책임을 분리하는 2단 구조**: `widgets/chat-more-panel/ui/ChatMorePanel.tsx`가 트리거 버튼(`SheetTrigger asChild`)과 `Sheet`(항목 리스트만) 자체를 통째로 소유해, 호출부(`ChatRoomView.tsx`)는 `<ChatMorePanel roomId={roomId} contentType={room.contentType} />` 하나만 헤더에 배치하면 된다(atom도 이 위젯 내부 `model/atom.ts`에만 있고 바깥에 노출하지 않음) — `ContentActionsMenu`(DropdownMenu 버전)와 동일한 "트리거+콘텐츠 자기완결형 위젯" 모양. 패널의 각 항목 클릭은 `setOpen(false)` 직후 해당 기능 전용 **react-call 모달**을 여는 것으로 끝난다(패널 자체는 react-call이 아님, 열림/닫힘 상태만 있는 목록이라 "호출→응답"이 필요 없다). 아직 구현되지 않은 항목(업데이트 정보/시작설정 변경/엔딩 컬렉션 등)은 `disabled` + 우측에 작은 `text-muted-foreground/60` "준비 중" 라벨로 렌더링해 목록 자체는 완전한 형태를 유지한다 — 다음 스토리에서 그 항목의 `active: true`와 클릭 핸들러만 채우면 된다.
- **(US-068) 입력/제출이 없는 순수 조회용 react-call 모달**은 `useMutationFlow`/`MutationFn` 없이 `createCallable<Props, void>(({ call, ...}) => ...)` + `open={!call.ended}` + `onOpenChange={(next) => !next && call.end()}`만으로 충분하다(`features/play-guide/ui/PlayGuideModal.tsx`) — 내부에서 `useChatRoomPlayGuideQuery(roomId, open)`처럼 그 모달이 열려있을 때만(`enabled`) 온디맨드로 쿼리하고, pending/error/empty 3상태는 `VersionHistoryModal`과 동일한 모양으로 그린다. `ConfirmChatRoomActionModal`/`ReportContentModal`(입력+제출이 있는 케이스)과 달리 이 경우는 `submit`/`pending` UI 자체가 없다.
- **(US-070) 방이 고정한 시작설정의 "물리적 PK"가 필요하면 `room.startingSetupId`(entity_id, 버전-안정 참조)를 쓰지 말고 `room.contentSnapshot?.pinnedStartingSetupId`를 쓴다** — `GET /stories/starting-setups/{id}/ending-collection`(US-069)처럼 물리적 PK를 path param으로 요구하는 엔드포인트를 호출할 때 처음 이 구분이 드러났다(`apps/api/CLAUDE.md`의 같은 항목 참고). `contentSnapshot`은 스토리 챗에서만 존재하므로 `startingSetupId`를 넘겨야 하는 액션(`ChatMorePanel`의 `startingSetupId?: string` prop 등)은 항상 optional로 받고, 캐릭터 챗/스냅샷이 아직 없는 순간엔 그 항목을 비활성 처리한다.
- **(US-070, 두 번째 react-call 모달의 "목록↔상세" 전환) 새 Dialog를 중첩하지 않고, 같은 `DialogContent` 안에서 로컬 state(선택된 아이템)로 두 뷰를 스위칭한다**(`features/ending-collection/ui/EndingCollectionModal.tsx`) — `VersionHistoryModal`(목록 전용)/`PlayGuideModal`(단일 콘텐츠 전용)과 달리 "여러 항목 중 하나를 골라 상세를 보고 다시 목록으로 돌아간다"는 패턴이 처음 필요했다. 전환 모션은 `features/sign-up`의 스텝 트랜지션과 동일한 관용구(`motion-safe:animate-in motion-safe:fade-in-0 motion-safe:slide-in-from-right-2 motion-safe:duration-200`)를 재사용했다 — Dialog `open`/`onOpenChange`는 항상 최상위 `call.end()`만 다루고, 목록↔상세 전환은 별도 로컬 state로 완전히 분리해 두면 된다.
- **(US-073, `entities/character-image-archive` 첫 등장) 소비 쿼리(US-074/075)가 아직 없어도 `characterImageArchiveKeys` 팩토리만 먼저 만들 수 있다** — `applyStreamEvent`(US-052)가 이미 노출해 둔 `opts.onDone` 확장 훅에 `features/send-message/model/useSendMessage.ts`가 처음으로 값을 채웠다: `useSendMessage(roomId, characterId?)`의 `characterId`는 캐릭터 챗일 때만(스토리 챗은 `room.contentType !== "character"`라 `undefined`) `ChatRoomView`가 `room.contentId`를 그대로 넘긴다. `onDone`은 `message.imageId && characterId`일 때만 `characterImageArchiveKeys.list(characterId)`를 invalidate하므로, 스토리 챗(`imageId` 항상 undefined)이나 캐릭터 챗 로드 이전 렌더(`characterId` undefined)에서는 그냥 no-op이다 — 별도 분기 없이 안전하다.
- **(US-073) BE가 `imageId`와 함께 `imageUrl`(presigned GET)도 내려주므로, FE는 이미지 보관함(US-074) 없이도 그 턴의 메시지에 바로 이미지를 렌더링한다** — `MessageBubble.tsx`가 `message.imageUrl`이 있을 때만 텍스트 말풍선 아래에 `<img>`를 추가로 쌓는다(`max-h-80 max-w-[75%] rounded-lg`, `object-fit` 없이 max-width/max-height만으로 원본 비율 유지, 크롭 없음). `entities/chat-room`의 `ChatMessage`/`toChatRoomState.ts`가 처음으로 `imageId`/`imageUrl`를 명시적으로 매핑하는 `toChatMessage()` 헬퍼를 얻었다 — 이전엔 `messages: dto.messages`로 그냥 그대로 넘기고 있었는데, BE의 nullable(`string | null`) 필드와 FE의 optional(`string | undefined`) 타입이 어긋나 있었다(이 스토리 이전엔 `imageUrl` 필드가 없어 드러나지 않던 잠재 타입 불일치 — 새 필드를 추가하며 함께 드러나 고쳤다).
- **(US-075, 세 번째 react-call 모달) 그리드 갤러리는 목록/단일 모달과 달리 상태 전환이 없어 `PlayGuideModal`과 동일한 최소 골격(`useMutationFlow` 없음, `open={!call.ended}` + `onOpenChange`로 `call.end()`)에 `grid grid-cols-3 gap-2` + `aspect-square` 셀만 얹으면 된다**(`features/image-archive/ui/ImageArchiveModal.tsx`, `useCharacterImageArchiveQuery(characterId, open)`). `exposed=false` 셀은 서버가 이미 블러 처리해 내려준 `imageUrl`(US-074) 위에 `absolute inset-0 bg-foreground/40` 스크림 + `Lock` 아이콘(`text-background`, 즉 흰색)만 얹는다 — 클라이언트 사이드 블러 처리 없음, 사진 위에서도 대비가 나오도록 `EndingCollectionModal`의 `text-muted-foreground/60`(흰 배경 리스트 전용) 대신 이 두 토큰 조합을 썼다. `ChatMorePanel`은 이제 `characterId?: string` prop도 받는다(`startingSetupId?`와 대칭 — 캐릭터 챗에서만 값이 오고, 스토리 챗의 `STORY_ITEMS`에는 애초에 이 항목이 없다).
- **(US-077) SSE 요청이 전송/재생성/수정 셋으로 늘어나면서 `shared/lib/sse/openChatStream.ts`의 `ChatStreamRequestPayload`를 `kind: "send"|"regenerate"|"edit"` 판별 유니언으로 일반화했다** — 각 kind가 메서드/URL/바디를 스스로 결정하므로, 새 SSE 액션이 추가돼도 이 함수 하나만 분기를 늘리면 된다(액션별 fetch 유틸을 따로 만들지 않는다). `entities/chat-room`의 대응 요청 타입(`SendMessageRequest`/`RegenerateRequest`/`EditMessageRequest`)도 같은 `kind` 필드로 구조적으로 맞춰 두었다 — `shared`가 `entities`를 import할 수 없는 FSD 제약(US-054 패턴) 때문에 명목상 다른 타입이지만 구조적으로 호환되게 유지하는 기존 관례 그대로다. `useSendMessage`의 `send`/`regenerate`/`editMessage`는 전부 `openStream({payload, mode})` 하나를 공유한다.
- **(US-077, 첫 인라인 편집 사례) 리스트 안의 항목 하나를 그 자리에서 짧게 고치는 액션은 Dialog를 열지 않고, 그 항목 컴포넌트 자체가 로컬 `isEditing` state로 "표시 모드"/"편집 모드"(Textarea + 저장/취소) 두 형태를 스위칭하게 한다**(`MessageBubble.tsx`) — 기존 폼형 모달(`EditProfileDialog`, US-035)과 달리 필드 하나뿐이고 컨텍스트(리스트 내 위치)를 유지해야 해서 모달을 여는 게 오히려 무겁다(impeccable product register의 "Modal as first thought는 지양, inline을 먼저 소진" 원칙과 일치). 편집 대상 식별은 호출부(`ChatRoomView`)의 단일 `editingMessageId` state가 관리하고, 항목 컴포넌트는 `isEditing`/`onSaveEdit`/`onCancelEdit`만 props로 받는다 — 다음에 비슷한 "목록 항목 즉석 수정" 케이스가 생기면 이 패턴을 재사용할 것.
- **(US-079) `UpdateInfoModal`은 `roomId`만 props로 받아 `useChatRoomQuery(roomId)`로 `chatRoomKeys.detail` 캐시를 자체 구독한다** — `PlayGuideModal`(US-068)과 동일하게 "이미 부모(`ChatRoomView`)가 로드해둔 캐시를 그대로 읽는" 패턴이라 별도 prop 스레딩이 필요 없다. 이 모달은 조회 전용(`PlayGuideModal`)과 확인+뮤테이션(`ConfirmChatRoomActionModal`)의 중간 형태(조회 정보를 보여주다가 조건부로 액션 버튼이 나타남)라 `useMutationFlow`를 쓰지 않고 `usePinLatestVersionMutation`을 직접 호출한다 — 성공 시 토스트 + `call.end()`로 닫는다. `usePinLatestVersionMutation`은 `useResetChatRoomMutation`(US-056)과 동일한 이유로 `invalidateQueries` 대신 `setQueryData`로 응답 전체를 캐시에 바로 꽂는다(엔드포인트가 이미 최신 상태의 `ChatRoomResponse` 전체를 반환하므로 재조회가 낭비).
- **(US-079) "서버 플래그가 뮤테이션 성공 즉시 꺼지는" 1회성 알림 배너는 그 플래그를 렌더링 조건으로 직접 쓰면 안 된다** — `room.versionAutoUpgraded`를 그대로 조건으로 쓰면 `useAcknowledgeVersionUpgradeMutation`이 캐시에서 그 값을 `false`로 되돌리는 순간 배너가 뜨자마자 사라진다. `ChatRoomView`는 대신 로컬 `versionUpgradeBannerVisible` state로 "봤다"는 사실을 분리해서 들고, `room`이 비동기로 로드되는 상황(`usePlayContent`의 US-046 메모와 동일한 이유로 `useEffectOnce`가 아니라)이라 `useRef` 가드 + `useEffect(() => {...}, [room])`으로 정확히 한 번만 배너를 켜고 acknowledge 뮤테이션을 호출한다 — 서버 플래그를 소비하는 로컬 state와 그 배너를 "보여줄지"를 결정하는 로컬 state를 반드시 분리할 것.
- **(US-081) `room.startingSetupId`(entity_id)와 `GET /contents/{id}`의 `startingSetups[].id`(물리적 PK)는 서로 다른 id 공간이라 "현재 선택된 설정"을 그 둘로 직접 비교하면 안 된다** — US-070이 이미 만들어 둔 `room.contentSnapshot.pinnedStartingSetupId`(물리적 PK)가 정확히 이 비교를 위한 필드였다(그 스토리의 예견이 이번에 처음 실사용됨). "방이 고정한 물리적 X"가 필요한 다음 기능도 이 필드부터 확인하고, `room.startingSetupId`를 물리적 PK 대신 쓰지 않는다.
- **(US-081) `Promise<boolean>`을 반환하는 react-call 확인 모달의 첫 사례**: `ConfirmChatRoomActionModal`(`Promise<void>`, `mutationFn`을 컴포넌트가 직접 실행)과 달리 "확정 후 실제 동작(뮤테이션 호출+navigate)을 호출부가 그대로 이어받아야 하는" 케이스는 `createCallable<void, boolean>` + `call.end(true/false)`로 별도 컴포넌트(`features/change-starting-setup/ui/ConfirmStartingSetupChangeModal.tsx`)를 만드는 게 더 단순했다. `createCallable`의 `Props` 기본값이 `void`라 `Modal.call()`을 인자 없이 호출할 수 있다. 확정 후 응답값(새 id 등)에 따라 후속 액션이 필요한 확인 모달은 이 패턴(boolean 반환 + 호출부가 후속 처리)을 재사용하고, 결과와 무관하게 단순 확인/취소면 계속 `ConfirmChatRoomActionModal`을 쓸 것.
- **(US-090) `SingleRule`/`RuleGroup`/`RuleListItem`/`ComparisonOp`/`LogicOp`와 `evaluateItem`/`evaluateRuleList`(techspec-builder-story.md §1.5, BE 포팅은 `apps/api/src/api/chat/ending_rules.py`)의 단일 진실 공급원은 `shared/lib/rule-engine/types.ts`다** — `entities/chat-room/api/types.ts`가 US-052부터 갖고 있던 동일 이름의 provisional 타입 정의는 이 모듈을 `export type {...} from`으로 재export하는 것으로 교체했다(중복 정의 금지, 위 US-052 항목 참고). 빌더(US-091~)/미리보기가 이 규칙 트리를 다루는 새 코드는 항상 이 모듈을 import할 것 — `entities/chat-room`에 다시 로컬 정의를 두지 않는다. `noUncheckedIndexedAccess`(루트 tsconfig) 때문에 `evaluateRuleList`는 `items[i]` 인덱싱 대신 `const [first, ...rest] = items` 구조분해 + `for...of`로 순회한다.
- **(US-091, 첫 formToServer/serverToForm 페어) 빌더 폼 zod 스키마에서 "서버 계약상 값이 아직 없을 수 있는 필수 선택 필드"(예: `registration.genre`/`registration.target`)는 techspec 의사코드가 non-nullable `z.enum([...])`로 적어도 실제로는 `.nullable()`로 구현해야 한다** — `z.string().min(1)`처럼 추론 타입이 `string`인 필드는 `''` 같은 placeholder 기본값이 타입은 통과하고 런타임 검증만 실패하지만(`sign-up`/`edit-profile` 기존 관례), `z.enum([...])`의 추론 타입은 "선택 안 됨"을 표현할 멤버가 아예 없는 리터럴 유니언이라 `.nullable()` 없이는 `serverToForm`이 실제 서버의 `null`(초안 상태) 값을 타입 안전하게 담을 방법이 없다. 발행 시 필수 여부 강제는 이 스키마를 쓰는 이후 스토리(발행 버튼/제출 검증)가 별도로 처리할 몫 — `profile.image`(이미 nullable, techspec-builder-story.md §2가 명시)와 정확히 같은 종류의 draft-vs-publish 분리다.
- **(US-091) 빌더 폼의 배열 항목 순서(`order`)가 서버 응답/요청 스키마에 명시적 숫자 필드로 없을 수 있다** — `situationalImages`처럼 배열 인덱스 자체가 우선순위인 필드는 BE가 `PATCH`에서 배열 위치로 `order` 컬럼을 갱신하고 `GET`에서 이미 `order` 정렬된 배열을 내려주므로, `formToServer`/`serverToForm`은 그냥 배열을 그대로 매핑하면 되고 별도 정렬/숫자 계산이 필요 없다. 스토리 빌더(US-092~095)의 시작설정/스탯/엔딩·규칙 배열도 같은 모양(order는 배열 위치, 명시 필드 없음)일 가능성이 높으니 실제 생성된 타입(`packages/api-types/src/generated.ts`)으로 먼저 확인할 것 — techspec 의사코드 문구("order 값으로 변환")만 보고 숫자 필드를 만들려 하지 말 것.
- **(US-092, 스토리 빌더 스키마가 US-092~095 4개 스토리에 걸쳐 `features/build-story/model/schema.ts` 한 파일에 누적된다) 각 스토리는 그 스토리가 담당하는 섹션만 `storyBuilderSchema`에 추가하고, `formToServer`/`serverToForm`의 반환/입력 타입도 `Pick<StoryDraftPayload/Response, [그 시점까지 다루는 필드]>`로 좁혀둔다** — `StoryDraftPayload.startingSetups`/`keywordNotes`/`shortcuts`는 와이어상 필수(non-optional) 배열이지만 아직 폼에 없는 섹션을 `[]` 같은 임시값으로 채워 넣지 않는다(다음 스토리가 실제 매핑을 추가하기 전까지 `PATCH` 페이로드 자체를 부분 타입으로 남겨두는 것). US-092가 `profile`/`storySetting`/`registration`을 먼저 구현했으니, US-093(startingSetups/statDefs)/094(keywordNotes/shortcuts)/095(endings/rules)는 이 파일에 필드를 이어서 추가하고 `Pick<...>`을 그만큼 넓히면 된다 — 095가 끝나면 `Pick`이 필요 없는 완전한 `StoryDraftPayload`/`StoryDraftResponse`에 도달한다.
- **(US-092) techspec 의사코드의 zod 필드명이 실제 생성된 와이어 타입과 다를 수 있다는 US-091 gotcha가 여기서도 재확인됐다** — `techspec-builder-story.md` §1의 `storySetting.worldSetting`은 실제 `StoryDraftPayload`/`StoryDraftResponse`(`packages/api-types/src/generated.ts`)에서 `settingText`라는 이름이다. 폼 필드명은 techspec 그대로(`worldSetting`) 유지하고 `formToServer`/`serverToForm`이 그 rename을 전담한다 — 새 zod 스키마를 작성할 때마다 techspec 코드블록만 믿지 말고 `generated.ts`에서 실제 필드명을 먼저 확인할 것.
- **(US-095, `storyBuilderSchema`가 endings/rules를 마지막으로 추가하며 완성됐다) zod v4에서 `z.discriminatedUnion(...)`의 각 멤버를 미리 `const x: z.ZodType<SomeType> = z.object({...})`로 넓혀두면 그 넓혀진 값을 다시 discriminatedUnion에 넣을 때 타입 에러가 난다** — `$ZodTypeDiscriminable`(discriminant 리터럴 추론에 쓰는 내부 캐퍼빌리티)가 `ZodType<T>` 어노테이션을 거치며 지워지기 때문(zod v3에서 흔했던 "명시적 `ZodType<T>` 어노테이션으로 재사용 타입 강제" 패턴이 v4 discriminatedUnion에서는 깨진다). `features/build-story/model/schema.ts`의 `singleRuleSchema`/`ruleGroupSchema`/`ruleListItemSchema`는 그래서 명시적 어노테이션 없이 자연 추론에 맡기고, "`shared/lib/rule-engine`의 타입을 그대로 재사용"이라는 요구는 대신 `formToServer.ts`/`serverToForm.ts`의 함수 파라미터 타입으로 스키마의 `z.infer` 결과를 그 타입 자리에 흘려보내는 방식(구조적 호환성이 어긋나면 그 지점에서 컴파일 에러)으로 검증한다.
- **(US-096, `features/build-common` 첫 등장) 캐릭터/스토리 빌더가 공유하는 자동저장은 `features/build-common/model/useAutosave.ts`** — techspec-builder-common.md §1 시그니처(`subscribe`/`formToServer`/`save`/`debounceMs`) 그대로, RHF `watch`를 `subscribe`로 주입해 필드 변경 시 디바운스(기본 1500ms) PATCH, `saveNow(values)` 호출("임시저장" 버튼)은 디바운스를 취소하고 즉시 저장한다. 리포지토리에 `lodash`가 의존성으로 없어 `.cancel()`을 갖는 debounce를 이 파일 안에 직접 구현했다 — `react-use`의 `useDebounce`(US-045가 좋아요 토글에 쓴 것)는 값 변경을 감지해 이펙트를 지연 실행하는 훅이라 "임의 시점에 즉시 저장으로 전환"이 안 되므로 이 용도에 맞지 않는다. 각 빌더는 이 훅에 자신의 `formToServer`(US-091~095)만 주입해 재사용할 것 — 디바운스/즉시저장 로직을 다시 구현하지 않는다.
- **(US-095) `shared/lib/rule-engine`의 `ComparisonOp`(6개: `>`, `>=`, `<`, `<=`, `==`, `!=`)와 실제 BE DB enum `EndingRuleOperator`(`techspec-db-schema.md` §5, `apps/api/src/api/db/models/story.py`)는 값 개수가 다르다 — BE는 `gte`/`lte`/`eq`/`gt`/`lt` 5개뿐이고 "not-equal"에 대응하는 값이 아예 없다.** `ComparisonOp` 자체는 US-090이 techspec §1.5 의사코드를 그대로 옮긴 평가 엔진 타입이라 안 바꾼다(`!=` 평가 로직 자체는 정상 동작). 대신 빌더가 실제로 만들어낼 수 있는 규칙의 연산자만 `schema.ts`의 `comparisonOpSchema`에서 5개로 좁혀서 애초에 "!="를 선택 불가능하게 막았다(`RuleListItemValues`/`SingleRuleValues` export, `ComparisonOp`보다 좁은 부분집합). 이 덕분에 `formToServer.ts`/`serverToForm.ts`의 연산자 매핑 `Record`는 방어 코드 없이 5개 키만으로 완전(exhaustive)하다 — 다음에 규칙 연산자 선택 UI(빌더 엔딩 화면)를 만들 때도 `ComparisonOp` 전체가 아니라 이 좁힌 타입/스키마를 옵션 소스로 쓸 것.
- **(US-097, `features/select-generated-image` 첫 등장) 캐릭터/스토리 빌더가 공유하는 이미지 필드(`GeneratedImageField`)와 피커 모달(`GeneratedImagePickerModal`, react-call)** — 업로드/갤러리선택/삭제 세 경로 모두 `{assetId} | null`(`profile.image`/`situationalImages[].image`와 동일 shape) 하나로 수렴한다. `GET /me/generated-images`(`apps/api/src/api/assets/router.py`의 `me_router`)는 techspec-backend-media.md §3이 명시한 대로 AI 이미지 생성 자체가 별도 세션 범위라 로그인만 확인하고 항상 빈 배열을 반환하는 스텁이다 — 실제 생성 파이프라인이 생기면 이 쿼리만 채우면 된다. `previewUrl` prop은 이미 서버에 저장된 값(assetId만 있고 URL은 없는 기존 draft-vs-publish 갭, apps/api CLAUDE.md 참고)을 편집할 때 소비자가 렌더링용 URL을 알고 있을 때만 넘기는 표시 전용 값이고, 이번 세션에 직접 업로드/선택한 이미지는 컴포넌트 로컬 상태(blob URL 또는 갤러리 아이템의 URL)가 항상 우선한다. 다음에 이 필드를 실제로 배선하는 스토리(US-100+)는 새 컴포넌트를 만들지 말고 이걸 import할 것(techspec-builder-story.md §2가 캐릭터/스토리 공용으로 명시).
- **(US-097) "새 탭에서 별도 페이지를 열되 현재 폼 상태에 영향을 주지 않는" 진입점은 새 라우트/네비게이션 로직 없이 평범한 `<a target="_blank" rel="noopener noreferrer">`로 충분하다** — `GeneratedImagePickerModal`의 "새로 생성하기" 링크가 `/studio/images`(`routes/studio.images.tsx`, `ComingSoonPage` 스텁 — 실제 생성 UX는 techspec-builder-story.md §4가 별도 범위로 명시)를 여는데, 별도 `window.open` 호출이나 상태 보존 로직이 필요 없다(브라우저의 새 탭 자체가 완전히 독립된 실행 컨텍스트라 원래 탭의 React 상태는 애초에 영향받을 수 없다).
- **(US-099, `entities/preview-session`/`features/preview-chat`/`widgets/preview-session` 첫 등장) 마운트 시 `useEffect`에서 곧바로 `.mutate(variables, { onSuccess })`를 호출하는 패턴은 React StrictMode(개발 모드)에서 그 결과를 영구히 잃을 수 있다** — `PreviewSessionView`의 자동 세션 시작이 실측 사례: StrictMode의 마운트→언마운트→재마운트 시뮬레이션이 `useMutation()`의 내부 `MutationObserver` 구독을 그 첫 `mutate()` 호출과 같은 동기 구간에서 해제했다가 재구독하는데, TanStack Query v5는 구독 해제 시점에 `hasListeners() === false`이면 그 Observer를 진행 중이던 Mutation에서 영구히 `removeObserver`한다(`node_modules/@tanstack/query-core/src/mutationObserver.ts`의 `onUnsubscribe`) — 이후 그 뮤테이션이 실제로 성공/실패해도 이 Observer는 알림을 받지 못해 `mutate()`의 콜백 인자(`onSuccess`/`onError`)도, 반응형 `result.isPending`/`result.data`도 전부 그 순간 값에 영구히 멈춘다(브라우저 콘솔 로그로 실측 확인, 프로덕션 빌드는 StrictMode가 없어 영향 없음). 해결: `widgets/content-detail/lib/usePlayContent.ts`의 `start()`가 이미 쓰던 대로 `mutateAsync()` + `await`로 결과를 직접 받는다 — 그 프로미스는 Mutation 자신의 `execute()`가 반환하는 것이라 Observer 알림 경로와 무관하게 정상 settle된다. 단, `mutateAsync`도 `useMutation()`이 반환하는 반응형 `isPending`/`data`는 여전히 같은 Observer를 거치므로(내부적으로 같은 `mutate` 메서드), 로딩/버튼 disabled 등 UI 상태는 그 훅의 `isPending`이 아니라 직접 관리하는 로컬 state(예: `useState` + `try/finally`)로 판단할 것 — `startMutation.isPending`을 렌더링 조건에 쓰면 첫 자동 실행 이후 그 상태가 영구히 `true`로 멈춰 버튼이 계속 비활성화된 채로 남는다. **마운트 시 뮤테이션을 실행하는 다음 코드(예: US-100의 `/builder/character/new` 진입 시 `POST /contents` 초안 자동 생성)도 이 패턴(mutateAsync+await, 로컬 로딩 state)을 기본으로 쓸 것.**
- **(US-099) `MutationObserver`가 성공적으로 attach된 뮤테이션(hasListeners 문제와 무관하게 정상 처리된 경우)의 hook-level `onSuccess`(즉 `useMutation({ onSuccess })`로 등록한 콜백, `.mutate(vars, { onSuccess })`의 두 번째 인자가 아니다)는 Mutation 인스턴스 자신의 `execute()` 안에서 직접 호출되므로 Observer 구독 여부와 무관하게 항상 실행된다** — `useStartPreviewMutation`의 `onSuccess: (state) => queryClient.setQueryData(...)`가 위 StrictMode 버그 상황에서도 계속 정상 동작한 이유. 쿼리 캐시를 채우는 등 "결과와 무관하게 항상 일어나야 하는 부수효과"는 훅 정의 시점의 `onSuccess`에 두고, "그 특정 호출의 결과값을 이 컴포넌트가 지역적으로 알아야 하는" 경우에만 `mutateAsync`의 반환값을 쓸 것 — 후자를 `.mutate()`의 콜백 인자로 얻으려 하면 위 버그에 노출된다.
- **(US-098, `features/submit-appeal` 첫 등장) `AppealModal`(react-call)은 `{ target: { kind: 'publish-rejection'; rejectionId } | { kind: 'moderation-action'; actionId } }` 하나만 props로 받아 자체적으로 `POST /appeals`를 호출하고 성공 시 토스트+`call.end()`까지 스스로 처리한다** — `ReportContentModal`(호출부가 `mutationFn`을 주입)이 아니라 `UpdateInfoModal`(자체 mutation 직접 호출)과 같은 계열이다: target이 이미 요청 바디 전체(`targetKind`/`targetId`)를 결정하고, 성공 후 동작이 "토스트+닫기"로 호출부와 무관하게 항상 동일해 호출부 삽입형 `mutationFn`을 요구할 이유가 없었다(action 모달을 새로 만들 때 "호출부마다 후속 동작이 갈리는가"로 두 계열 중 하나를 고를 것). **아직 실제 소비 화면이 없다** — publish-rejection 진입점은 캐릭터/스토리 빌더의 발행 연동(US-105/114)이, moderation-action 진입점(techspec-builder-common.md §5.2, 알림 벨 클릭)은 이 저장소의 PRD 백로그에 아직 그 배선을 맡을 스토리가 없다(원본 PRD의 US-055에 해당하는 항목이 Ralph 변환 과정에서 별도 스토리로 남지 않은 것으로 보임 — `widgets/header/ui/NotificationBell.tsx`의 `onSelect`는 지금도 읽음 처리만 하고 이 모달을 열지 않는다). 다음에 헤더 알림 벨을 다시 만질 일이 있으면 이 갭을 함께 메울 것. US-042/097 선례대로 `/ui-demo`에 임시 데모 섹션(거부 사유 텍스트+"이의제기" 버튼)을 추가해 실제 로그인 사용자로 `POST /appeals` 왕복(DB row까지 직접 조회해 확인)을 검증한 뒤 그 페이지 변경분만 원복했다.
- **(US-100, 첫 빌더 셸 — `widgets/build-character`) TanStack Router 파일 라우트에서 정적 세그먼트와 동적 파라미터가 같은 위치에서 경쟁하면(`routes/builder.$type.new.tsx` vs 기존 `routes/builder.$type.$draftId.tsx`) 정적 세그먼트가 항상 우선 매치된다** — `/builder/character/new`가 `$draftId="new"`로 흡수되지 않고 `new.tsx` 쪽으로 정확히 라우팅됨을 브라우저로 실측 확인했다. 두 라우트를 같은 부모 경로 아래 나란히 둘 때 별도 순서 지정이나 우선순위 설정이 필요 없다. `builder.$type.$draftId.tsx`의 `$type`은 `content.$type.$id.tsx`(US-043)와 동일하게 URL 가독성용 세그먼트일 뿐이다 — `GET /contents/{id}/draft` 응답 자체의 `type` 판별값으로 `CharacterBuilderShell`/스토리(아직 `ComingSoonPage`, US-106 예정) 렌더링을 나눈다. 반대로 `builder.$type.new.tsx`는 `$type`이 `POST /contents`의 payload를 결정하므로 실제로 사용한다.
- **(US-100) 마운트 시 뮤테이션(`POST /contents` 초안 자동생성) 구현은 US-099가 예견한 대로 `mutateAsync`+`await`+로컬 `useState`(`hasFailed`)로 작성했다** — `createDraftMutation.isPending`을 렌더링 조건으로 쓰지 않는다. 이 패턴이 이제 두 번째 사례(첫 사례는 `PreviewSessionView`)로 확인됐으니, 마운트 시 자동 실행되는 다음 뮤테이션도 기본값으로 채택할 것.
- **(US-100) 빌더처럼 여러 스토리에 걸쳐 탭이 늘어나는 화면은 발행 가능 여부를 RHF `formState.isValid`/개별 `trigger()`가 아니라 `builderSchema.safeParse(useWatch({ control }))`로 판단한다** — 아직 미구현 탭의 필수 필드(예: US-100 시점의 `intro.firstMessage`/`registration.description`)가 항상 빈 문자열이라 자연스럽게 발행 버튼이 비활성 상태를 유지하고, 각 탭 스토리가 실제 입력 UI를 추가할 때마다 스키마 자체는 안 건드려도 버튼 활성 조건이 점진적으로 정확해진다. 이 폼엔 아직 `zodResolver`를 연결하지 않았다(제출 액션 자체가 없어 트리거할 일이 없음, US-105가 발행 로직을 연결할 때 트리거 방식이 필요해지면 그때 추가) — 인라인 필드 에러 메시지 없이 라벨의 `*` 표시만으로 필수 여부를 안내한다.
- **(US-100) `packages/ui`에 shadcn `tabs`를 처음 추가했다** — 빌더처럼 화면 내 뷰를 전환하는 표준 내비게이션엔 새로 만들지 않고 그대로 쓴다. `TabsList`의 `variant="line"`(밑줄형, 기본값인 `variant="default"`의 박스+`shadow-sm` 대신)을 골랐다 — DESIGN.md의 Flat-at-Rest 규칙(카드/버튼/인풋은 정지 상태에서 그림자 없음, 그림자는 다이얼로그류 오버레이 전용)과 The One Accent Rule(`primary`는 화면당 하나의 주 액션 전용, 여기선 발행 버튼)에 맞춰 활성 탭 표시를 `primary`가 아니라 `foreground` 밑줄로 했다 — 앞으로 탭이 필요한 화면(스토리 빌더 US-106+, 관리자 화면 등)도 이 컴포넌트를 그대로 재사용하고 `variant="line"`을 기본으로 우선 고려할 것.
- **(US-101, 첫 `useFieldArray` 사용) 순서가 의미 없는 배열 필드(add/remove만 필요, 재정렬 불필요)는 dnd-kit 없이 RHF `useFieldArray` + `fields.map((field, index) => ...)`(`key={field.id}`)로 충분하다** — `register(`intro.exampleDialogues.${index}.userLine`)`처럼 인덱스를 문자열 템플릿에 꽂아 배열 항목 필드를 등록하고, `append({ id: crypto.randomUUID(), ... })`로 새 항목에 클라이언트 uuid를 채운다(`entities/chat-room`/`entities/preview-session`이 이미 쓰던 `crypto.randomUUID()` 관례와 동일). 순서 자체가 우선순위인 배열(situationalImages 등, US-103)은 이 패턴만으로 부족하니 착수 시 다시 판단할 것.
- **(US-101) "토글로 섹션 전체를 숨김/노출"하는 로컬 UI state는 폼 값과 분리해서 `useState(() => fields.length > 0)`(또는 그 필드의 초기값 기준)로 초기화한다** — 서버가 이미 값을 갖고 있으면 처음부터 펼쳐진 상태로 보여주고, 토글을 껐다 켜도 데이터 자체는 건드리지 않는다(끌 때 배열/필드를 비우면 오프 상태에서 실수로 값이 날아가므로 하지 않는다).
- **(US-101) `packages/ui`에 shadcn `switch`를 처음 추가했다** — 즉시 on/off를 표현하는 단일 스위치(고급설정 토글 등)가 필요하면 이걸 재사용할 것. `Toggle`은 눌림 상태를 표현하는 버튼형이라 의미가 다르고, `Checkbox`는 폼 필드 체크용이다. CLI 원본의 `dark:` variant 클래스는 다크모드 미구현 관례(`packages/ui/CLAUDE.md`)대로 제거하고 추가했다 — 새 shadcn 컴포넌트를 추가할 때마다 이 클래스가 남아있는지 확인할 것.
- **(US-103, 첫 dnd-kit 사용) 순서 자체가 우선순위인 배열(상황별 이미지)은 `@dnd-kit/core`+`@dnd-kit/sortable`+`@dnd-kit/utilities` + RHF `useFieldArray`의 `move(from, to)`로 재정렬한다** — `DndContext`(`sensors=[useSensor(PointerSensor)]`, `collisionDetection={closestCenter}`)가 목록 전체를 감싸고, `SortableContext`(`items={fields.map(f => f.id)}`, `verticalListSortingStrategy`)가 그 안에 오며, 각 행은 `useSortable({ id: field.id })`로 `attributes`/`listeners`(드래그 핸들 전용 엘리먼트에만 스프레드 — 행 전체에 걸면 텍스트 입력이 막힌다)/`transform`/`transition`을 얻어 `style={{ transform: CSS.Transform.toString(transform), transition }}`을 루트에 적용한다. `onDragEnd`는 `fields.findIndex(f => f.id === active.id / over.id)`로 인덱스를 구해 `move(oldIndex, newIndex)` 하나만 호출하면 된다 — RHF가 각 행의 값과 안정적인 `field.id`를 함께 옮겨주므로 별도 배열 상태가 필요 없다. **주의**: chrome-devtools MCP의 범용 `drag` 툴(엘리먼트 uid→uid)은 dnd-kit의 `PointerSensor`(실제 pointerdown/pointermove/pointerup 이벤트 필요)에 반응하지 않는다 — 브라우저 검증 시 재정렬이 안 먹히면 `evaluate_script`로 직접 `PointerEvent`(pointerdown → 여러 단계 pointermove → pointerup, 각각 `bubbles:true`/`pointerId`/`isPrimary:true`) 시퀀스를 디스패치해서 확인할 것(코드 버그가 아니라 툴의 한계).
- **(US-103) `situationalImages`처럼 이미지 자산이 `PATCH /contents/{id}/draft`가 아니라 별도 엔드포인트(`POST /assets/{id}/register-situational-image`, US-071)로만 반영되는 필드는, 공용 `GeneratedImageField`(`features/select-generated-image`, 프로필 이미지 등이 씀 — 업로드 결과가 그대로 draft PATCH로 나가는 걸 전제)를 재사용하지 않는다.** 이런 필드는 (1) AI 생성 갤러리 진입점이 없고(업로드 전용) (2) 업로드 성공 직후 그 도메인 전용 등록 엔드포인트를 추가로 호출해야 서버에 실제로 반영된다는 두 가지 이유로 전용 컴포넌트가 필요하다(`widgets/build-character/ui/AdvancedTab.tsx`의 `SituationalImageRow`, `shared/lib/asset/registerSituationalImage.ts`). 그 등록 호출에 필요한 `contentVersionId`가 draft 응답 어디에도 없어서 `CharacterDraftResponse`에 새로 추가했다(US-020/022/030/042/030의 "FE가 다른 방법으로 얻을 수 없는 값이면 응답에 필드를 추가" 판단과 같은 종류) — `apps/api/CLAUDE.md` 참고. 등록 호출은 "노출 상황" 텍스트가 비어있으면(서버가 `trigger_condition` 필수) 422가 나므로, 업로드 핸들러가 호출 전에 그 필드 값을 확인해 비어있으면 토스트로 막는다.
- **(US-103) `assetId`만 있고 렌더링 가능한 URL이 없는(US-034 이후 반복된 기존 갭) 기존 값을 다시 열었을 때는, 그 갭을 다시 메우려 하지 말고 "등록됨" 같은 텍스트/아이콘 placeholder로 충분하다** — 이 스토리에서 `CharacterSituationalImageItem`에 `imageUrl`을 추가로 얹을 수도 있었지만, `ProfileTab`의 대표 이미지도 아직 이 갭이 안 고쳐진 채로 남아있어(추가 `previewUrl` prop 자체를 안 넘김) 굳이 이 필드 하나만 먼저 고치지 않았다 — 여러 곳에 걸친 기존 갭은 그 갭을 실제로 아프게 느끼는 화면(마이페이지 초안 카드처럼)이 나올 때 한 번에 판단할 것.
- **(US-105) `POST /contents/{id}/publish`는 요청 바디를 받지 않는다** — 서버에 이미 저장돼 있는 초안(직전 draft PATCH가 반영한 값)을 그대로 발행한다. 그래서 발행 버튼 핸들러는 반드시 `characterBuilderSchema.parse()` → `formToServer()` → **`useUpdateContentDraftMutation`으로 먼저 저장** → 그다음에야 `usePublishContentMutation()`(인자 없음)을 호출하는 순서여야 한다 — draft PATCH를 건너뛰고 바로 publish를 부르면 그 시점까지 debounce로 아직 안 나간 최신 폼 값이 발행에서 누락될 수 있다. 스토리 빌더(US-114)도 동일한 순서를 그대로 따를 것.
- **(US-105) `characterBuilderSchema`의 `profile.image`/`registration.genre`/`registration.target`은 초안 상태를 표현하려고 nullable이라(US-091), 발행 버튼의 `safeParse` 가드를 통과해도 서버(`validate_character_publish`)가 요구하는 필수값이 비어 있을 수 있다** — 즉 그 게이트만으로는 발행 가능 여부를 완전히 보장하지 못하는 알려진 간극이다. 이 스토리는 그 간극을 스키마를 더 엄격하게 좁히는 대신(AC가 `characterBuilderSchema.parse()`를 그대로 쓰라고 명시) BE의 400 `{missingFields: [...]}` 응답을 필드명→한국어 라벨(`MISSING_FIELD_LABELS`, `CharacterBuilderShell.tsx`)로 매핑해 토스트로 안내하는 쪽을 택했다 — 스토리 빌더도 같은 간극이 있을 것이므로 같은 매핑 패턴을 재사용할 것.
- **(US-105) 발행 API의 400 `detail`은 두 가지 모양 중 하나다 — `{missingFields: string[]}`(필수 입력 누락, 토스트로만 안내)와 `{reason: string}`(자동 필터 거부, 이의제기 진입점이 있는 배너로 안내)** — `"reason" in detail`로 후자를 먼저 판별하고, 아니면 `missingFields` 배열 여부로 전자를 판별한다. 이의제기 모달(`AppealModal`, US-098)의 `{kind: "publish-rejection", rejectionId}`에서 `rejectionId`는 별도 "발행거부 이력" 엔티티가 없으므로(`apps/api/CLAUDE.md`의 `Appeal.target_id` 항목 참고) 그냥 콘텐츠의 물리적 `id`(`CharacterDraftResponse.id`)를 그대로 넘기면 된다.
- **(US-105) 빌더 어디서든 열리는 "미리보기" 화면(`PreviewSessionView`, US-099)은 Dialog로 띄우지 않고, 빌더 셸 컴포넌트 안에서 로컬 `isPreviewOpen` state로 탭 UI 전체를 통째로 교체(early return)하는 방식으로 연다** — `PreviewSessionView`가 `h-[calc(100dvh-3.5rem)]`(전역 헤더 높이를 뺀 풀스크린, `ChatRoomView`와 동일 관례)로 이미 만들어져 있어 Dialog 오버레이로 감싸면 어색하고, 무엇보다 미리보기의 `getPayload`가 `form.getValues()`(무검증)를 그 자리에서 즉시 읽어야 하므로 같은 컴포넌트 트리 안에 살아있는 RHF 인스턴스가 필요하다 — 별도 라우트로 분리하면 그 인스턴스에 접근할 수 없다. `PreviewSessionView`에 optional `onClose` prop을 추가해(닫으면 다시 빌더 탭으로) 이 패턴을 지원하도록 했다 — 스토리 빌더(US-114)도 동일하게 재사용할 것.
- **(US-105) `features/build-common`의 `useAutosave().saveNow`가 이제 `opts.save(...)`의 프로미스를 그대로 반환한다** (원래는 fire-and-forget으로 반환값이 없었다) — "임시저장" 버튼처럼 사용자가 직접 누르는 액션은 성공/실패 토스트 피드백이 필요해서 호출부가 `await`할 수 있어야 했다. 자동저장(디바운스 경로)은 여전히 반환값을 무시해도 된다.
- **(US-105) GEMINI_API_KEY가 없는 로컬 환경에서 발행/미리보기 화면을 브라우저로 검증하려면, FastAPI의 `Depends()`가 라우트 본문 실행 전에 전부 resolve된다는 점(`apps/api/CLAUDE.md`의 SSE 항목 참고) 때문에 `missingFields`처럼 LLM을 안 타야 정상인 분기조차 `get_llm_client()` 생성 시점의 `ValueError`로 막힌다.** `apps/api/src/api/llm/dependencies.py`의 `get_llm_client()`를 임시로 `DEV_FAKE_LLM=1`이면 고정 응답(필터 통과/거부는 `DEV_FORCE_FILTER_REJECT=1`로 전환, 대화 생성은 고정 토큰 스트림)을 주는 페이크로 바꿔 uvicorn을 띄운 뒤, 브라우저 검증이 끝나면 `git checkout -- apps/api/src/api/llm/dependencies.py`로 완전히 원복했다(커밋 전 `git status`로 깨끗함 확인) — US-055가 SSE 검증에 썼던 "임시 수정 → 확인 → checkout으로 원복" 패턴과 동일, LLM 판단이 필요한 다음 화면(스토리 빌더 발행, US-114)도 이 방법을 재사용할 것.
- **(US-109, `shared/ui/color-icon-picker` 첫 등장) `grid grid-cols-N`으로 만든 옵션 패널을 `position: absolute`(`width: auto`) 컨테이너 안에 두면 근본적으로 폭이 0에 가깝게 붕괴한다** — Tailwind의 `grid-cols-N`은 `grid-template-columns: repeat(N, minmax(0, 1fr))`로 컴파일되는데, CSS Grid 스펙상 `minmax(0, ...)` 트랙은 grid의 intrinsic(shrink-to-fit) 폭 계산에 **0**으로 기여한다. `width`/`left`/`right`가 없는 절대배치 요소는 바로 이 intrinsic 계산으로 자기 폭을 정하므로, 실제로는 `getComputedStyle(...).gridTemplateColumns`가 `"2px 2px 2px 2px"`처럼 붕괴해 고정폭 자식(아이콘/색상 버튼)들이 겹쳐 보인다(`@source` 스캔 누락이 아니라 클래스는 정상적으로 CSS에 존재함 — 스크린샷으로 이상 발견 → `getComputedStyle`로 원인 확정하는 순서로 디버깅했다). **해결: 그런 패널엔 항상 명시적 `w-*`를 추가**한다(`ColorPicker.tsx`/`IconPicker.tsx`가 `w-48`/`w-40`). US-066의 `ShortcutAutocomplete` absolute 패널은 단일 컬럼 리스트라 이 버그를 우연히 피해갔을 뿐 — 다음에 만드는 absolute 팝오버가 `grid-cols-*`를 쓴다면 반드시 폭을 명시할 것.
- **(US-109) 부모 배열의 동적으로 선택된 인덱스에 `name`이 의존하는 중첩 `useFieldArray`(예: `startingSetups.${selectedIndex}.stats`)는, 선택이 바뀔 때마다 그 훅을 쓰는 하위 컴포넌트를 부모의 안정적인 id로 `key`를 줘서 통째로 remount해야 한다** — `useFieldArray`는 살아있는 훅 인스턴스에서 `name` 문자열이 바뀌는 것을 재구독하도록 설계돼 있지 않다. `<StatSection key={setup.id} startingSetupIndex={index} />`(`widgets/build-story/ui/StatTab.tsx`)가 첫 사례 — 배열 안의 배열을, 형제 선택 UI로 넘나드는 다음 화면도 이 패턴(부모 `id`로 key, 인덱스 자체로 key 주지 말 것 — 재정렬 시 인덱스가 바뀔 수 있음)을 재사용할 것.
- **(US-112, 재귀적 트리 배열을 그룹 안팎에서 같은 UI로 편집하는 첫 사례) 배열 항목이 discriminated union이고 그 중 한 variant가 "자기 자신과 구조적으로 유사한 하위 배열"을 갖는 경우(엔딩의 `statRules: (SingleRule | RuleGroup)[]`, `RuleGroup.rules: SingleRule[]`), RHF `useFieldArray`를 쓰지 않고 그 배열 편집 컴포넌트를 `items`/`onChange` prop만 받는 순수 제어 컴포넌트로 만들어 그룹 항목의 하위 배열에 자기 자신을 재귀 호출한다**(`widgets/build-story/ui/EndingTab.tsx`의 `RuleListEditor`, `allowGroups` prop으로 그룹 추가 버튼 노출 여부만 다르게 함 — techspec이 "그룹 안의 rules 배열도 동일한 재정렬 UI를 재사용"을 명시하면 정확히 이 모양). `useFieldArray`를 안 쓴 이유: discriminated union 배열의 동적 인덱스 경로(`statRules.${i}.rules`, i번째가 특정 variant일 때만 유효한 필드)는 RHF 경로 타입 추론이 감당하지 못한다 — 대신 상위에서 `useWatch`+`setValue`로 배열 전체를 얹고, add/remove/`@dnd-kit/sortable`의 `arrayMove`로 배열 자체를 교체하는 순수 함수형 갱신을 쓴다(`suggestedReplies`/`keywordNotes.scope`처럼 기존에도 있던 "non-fieldArray 배열/유니언은 setValue로 통째 교체" 관례의 연장). 좁은 하위 타입(`SingleRuleValues[]`)과 넓은 상위 타입(`RuleListItemValues[]`)이 만나는 write-back 지점에서만 `Array.prototype.filter`의 타입 가드로 좁혀준다(런타임엔 항상 no-op이지만 타입체커에 증명하는 유일한 지점).
- **(US-115) "호출부가 mutationFn을 주입" 계열(`ConfirmChatRoomActionModal`)과 "자체적으로 mutation을 호출" 계열(`UpdateInfoModal`/`AppealModal`) 중 어느 react-call 모달을 재사용할지는, 성공 후 동작이 호출부마다 갈리는지로 결정한다** — 비공개 전환(`features/make-content-private/ui/MakeContentPrivateModal.tsx`)은 프로필 카드와 상세화면 액션 메뉴 두 곳에서 호출되지만 성공 후 동작(토스트+캐시 무효화+닫기)이 완전히 동일해서, `ConfirmChatRoomActionModal`을 다른 도메인(content)에 억지로 cross-import하지 않고 `contentId`/`creatorUserId`만 받는 새 "자체 mutation 직접 호출" 모달을 만들었다. `entities/content`에 `contentKeys.listByUser(userId)`(타입/공개여부 필터와 무관하게 그 작가의 모든 목록 쿼리를 접두사로 무효화하는 헬퍼)를 추가했다 — 특정 필터 조합과 무관하게 한 사용자의 목록 쿼리 전체를 무효화해야 하는 다음 뮤테이션도 이 패턴(전체 키의 부분 접두사를 반환하는 `listByUser`류 헬퍼)을 재사용할 것.
- **(US-115) 카드 전체가 클릭 영역인 리스트 항목에 카드 자신과 다른 동작을 하는 두 번째 인터랙티브 엘리먼트를 추가하려면, 그 카드가 아직 `<button>`으로 남아있는지 먼저 확인한다** — `pages/profile/ui/ProfileContentSection.tsx`의 로컬 `ContentCard`는 US-044가 `entities/content/ui/ContentCard.tsx`에 확립한 `role="button" tabIndex={0}` `div`(+ Enter/Space `onKeyDown`) 패턴으로 아직 마이그레이션되지 않은 채 순수 `<button>`으로 남아있었다 — 그 안에 "비공개 전환" 버튼을 중첩하기 전에 먼저 이 패턴으로 바꿔야 했다(버튼 안에 버튼 중첩은 유효하지 않은 HTML). 이 두 `ContentCard` 구현(홈/즐겨찾기용 공용 컴포넌트, 프로필 전용 로컬 컴포넌트)의 통합은 여전히 미완료 상태로 남아있다(US-042/044 메모 참고) — 다음에 프로필 카드를 만질 일이 있으면 이 통합도 함께 고려할 것.
